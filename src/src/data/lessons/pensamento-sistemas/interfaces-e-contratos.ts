import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'interfaces-e-contratos',
  moduleId: 'pensamento-sistemas',
  title: 'Interfaces e Contratos',
  description: 'O que sao interfaces, por que importam mais que implementacoes e como definir contratos claros entre componentes',
  order: 3,
  estimatedMinutes: 25,
  sections: [
    {
      type: 'text',
      content:
        '## O que e uma Interface?\n\nUma interface e um **contrato**: define o que um componente promete fazer, sem dizer como vai fazer. E a lista de metodos que voce pode chamar, com suas entradas e saidas esperadas.\n\nPense num tomada eletrica: a interface e o formato dos pinos e a voltagem esperada. Voce pode conectar qualquer aparelho que respeite esse contrato — um secador brasileiro, uma TV japonesa, um carregador americano (com adaptador). A implementacao interna de cada aparelho e completamente diferente, mas todos respeitam o mesmo contrato.\n\n### Por que interfaces importam mais que implementacoes?\n\n**Codigo que depende de interfaces e flexivel.** Se voce escreve:\n```python\nsistema.enviar_notificacao(usuario_id, mensagem)\n```\n...voce nao se importa se por dentro isso usa email, SMS, push notification ou pombo-correio. Quando a implementacao mudar, seu codigo continua funcionando.\n\n**Codigo que depende de implementacoes e fragil.** Se voce escreve:\n```python\nsmtp_client.connect("smtp.gmail.com", 587)\nsmtp_client.login(usuario, senha)\nsmtp_client.send_message(msg)\n```\n...voce esta acoplado ao Gmail. Mudar para SendGrid exige reescrever tudo.',
    },
    {
      type: 'text',
      content:
        '## APIs como Contratos\n\nUma API (Application Programming Interface) e uma interface entre sistemas. Quando voce usa a API do Google Maps, voce nao sabe como o Google calcula rotas — voce apenas chama `calcular_rota(origem, destino)` e recebe o resultado.\n\n### Como definir uma boa interface\n\n**(1) Nomes que descrevem COMPORTAMENTO, nao implementacao**\n- Bom: `ServicoDeNotificacao.notificar(usuario_id, mensagem)`\n- Ruim: `ServicoDeEmail.enviar_via_smtp(destinatario, corpo)`\nO primeiro nome funciona para qualquer implementacao. O segundo esta amarrado ao SMTP.\n\n**(2) Parametros e retornos semanticos**\nUse os conceitos do dominio, nao detalhes tecnicos.\n- Bom: `RepositorioDeUsuarios.buscar(usuario_id) → Usuario`\n- Ruim: `RepositorioDeUsuarios.executar_query(sql) → list[dict]`\n\n**(3) Ocultar detalhes de implementacao**\nA interface nao deve expor detalhes que podem mudar: nomes de tabelas, URLs de servicos externos, estruturas internas de dados.\n\n**(4) Contrato explicito de erros**\nDocumente o que acontece quando algo da errado. `buscar_usuario(id)` retorna `None` ou lanca excecao se nao encontrar? Isso faz parte do contrato.',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: 'from abc import ABC, abstractmethod\n\n# INTERFACE (contrato)\n# Define o que qualquer servico de notificacao DEVE fazer\n# Nao diz COMO fazer\nclass ServicoDeNotificacao(ABC):\n    @abstractmethod\n    def notificar(self, destinatario: str, assunto: str, mensagem: str) -> bool:\n        """Envia uma notificacao.\n        Retorna True se enviado com sucesso, False caso contrario.\n        """\n        pass\n\n    @abstractmethod\n    def verificar_disponibilidade(self) -> bool:\n        """Verifica se o servico esta operacional."""\n        pass\n\n\n# IMPLEMENTACAO 1: Email\n# Respeita o contrato — implementa os metodos da interface\nclass NotificacaoPorEmail(ServicoDeNotificacao):\n    def __init__(self, servidor_smtp: str, porta: int):\n        self.servidor_smtp = servidor_smtp\n        self.porta = porta\n\n    def notificar(self, destinatario: str, assunto: str, mensagem: str) -> bool:\n        print(f"[EMAIL] Para: {destinatario}")\n        print(f"        Assunto: {assunto}")\n        print(f"        Mensagem: {mensagem}")\n        # Aqui estaria o codigo real de SMTP\n        return True\n\n    def verificar_disponibilidade(self) -> bool:\n        # Aqui verificaria conexao com servidor SMTP\n        return True\n\n\n# IMPLEMENTACAO 2: SMS\n# Mesma interface, implementacao completamente diferente\nclass NotificacaoPorSMS(ServicoDeNotificacao):\n    def __init__(self, numero_api: str):\n        self.numero_api = numero_api\n\n    def notificar(self, destinatario: str, assunto: str, mensagem: str) -> bool:\n        # SMS nao tem "assunto" — mas ainda respeita o contrato\n        texto_sms = f"{assunto}: {mensagem}"[:160]  # limite de 160 chars\n        print(f"[SMS] Para: {destinatario}")\n        print(f"      Texto: {texto_sms}")\n        return True\n\n    def verificar_disponibilidade(self) -> bool:\n        return True\n\n\n# CODIGO QUE USA A INTERFACE\n# Este codigo nao sabe se e email, SMS, ou qualquer outra coisa\n# Ele apenas usa o contrato ServicoDeNotificacao\ndef notificar_usuario_sobre_pedido(servico: ServicoDeNotificacao, email_usuario: str, pedido_id: str):\n    if not servico.verificar_disponibilidade():\n        print("Servico de notificacao indisponivel — pedido salvo para retry")\n        return False\n    return servico.notificar(\n        destinatario=email_usuario,\n        assunto="Pedido confirmado",\n        mensagem=f"Seu pedido {pedido_id} foi confirmado e esta sendo preparado!"\n    )\n\n\n# Teste com Email\nservico_email = NotificacaoPorEmail("smtp.gmail.com", 587)\nnotificar_usuario_sobre_pedido(servico_email, "ana@email.com", "P-001")\n\nprint()\n\n# Teste com SMS — mesma chamada, comportamento diferente\nservico_sms = NotificacaoPorSMS("api-key-twilio")\nnotificar_usuario_sobre_pedido(servico_sms, "+5511999999999", "P-002")',
        filename: 'interface_notificacao.py',
        description:
          'A funcao notificar_usuario_sobre_pedido nao sabe se esta usando email ou SMS — ela apenas chama o contrato. Para adicionar WhatsApp amanha, criamos NotificacaoPorWhatsApp sem mudar uma linha do codigo existente.',
      },
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'Principio de Liskov (do SOLID): se seu codigo funciona com a interface base, deve funcionar com QUALQUER implementacao dessa interface — sem surpresas. Se NotificacaoPorSMS se comporta de forma inesperada quando voce troca por ela, a interface esta mal definida ou a implementacao esta violando o contrato.',
    },
    {
      type: 'text',
      content:
        '## Interfaces em APIs Web (REST)\n\nO mesmo conceito se aplica a APIs HTTP. Quando voce define uma API REST, voce esta definindo um contrato publico:\n\n```\nGET  /usuarios/{id}        → retorna Usuario ou 404\nPOST /usuarios             → cria Usuario, retorna 201 com o novo usuario\nPUT  /usuarios/{id}        → atualiza Usuario ou 404\nDELETE /usuarios/{id}      → remove Usuario ou 404\n```\n\nEsse contrato e independente de linguagem, banco de dados ou framework. O frontend pode ser React, Flutter ou terminal — todos usam o mesmo contrato. O backend pode ser Python, Java ou Go — todos respondem ao mesmo contrato.\n\n**Nunca quebre contratos publicos.** Se sua API ja retorna `{"nome": "Ana"}` e voce muda para `{"name": "Ana"}`, voce quebrou todos os clientes que ja integraram. Versione sua API: `/v1/usuarios` e `/v2/usuarios` podem ter contratos diferentes.',
    },
  ],
  challenges: [
    {
      id: 'interfaces-c1',
      title: 'Implemente Duas Formas de Armazenamento com a Mesma Interface',
      description:
        'Defina uma interface RepositorioDeUsuarios e implemente-a de duas formas: em memoria (usando dict) e "em arquivo" (simulado com print). O codigo que usa o repositorio nao deve precisar mudar ao trocar uma implementacao pela outra.',
      language: 'python',
      starterCode:
        '# Defina a interface e duas implementacoes\nfrom abc import ABC, abstractmethod\n\n\n# PASSO 1: Defina a interface (classe abstrata)\nclass RepositorioDeUsuarios(ABC):\n    """Contrato: qualquer repositorio de usuarios DEVE implementar estes metodos"""\n\n    @abstractmethod\n    def salvar(self, usuario_id: str, nome: str, email: str) -> None:\n        """Salva um novo usuario"""\n        pass\n\n    @abstractmethod\n    def buscar(self, usuario_id: str) -> dict | None:\n        """Retorna o usuario ou None se nao encontrado"""\n        pass\n\n    @abstractmethod\n    def listar_todos(self) -> list[dict]:\n        """Retorna todos os usuarios"""\n        pass\n\n    @abstractmethod\n    def remover(self, usuario_id: str) -> bool:\n        """Remove o usuario. Retorna True se removido, False se nao existia"""\n        pass\n\n\n# PASSO 2: Implemente RepositorioEmMemoria\n# Usa um dicionario interno para armazenar os dados\nclass RepositorioEmMemoria(RepositorioDeUsuarios):\n    def __init__(self):\n        self._dados = {}  # {usuario_id: {"nome": str, "email": str}}\n\n    # Implemente os 4 metodos aqui\n    pass\n\n\n# PASSO 3: Implemente RepositorioSimuladoEmArquivo\n# Em vez de realmente gravar em arquivo, apenas printe o que faria\n# Mantenha um dict interno para buscar funcionar\nclass RepositorioSimuladoEmArquivo(RepositorioDeUsuarios):\n    def __init__(self, nome_arquivo: str):\n        self.nome_arquivo = nome_arquivo\n        self._cache = {}  # simula o conteudo do arquivo\n\n    # Implemente os 4 metodos aqui — ao salvar/remover, printe a operacao\n    pass\n\n\n# PASSO 4: Esta funcao usa APENAS a interface — nao sabe qual implementacao esta usando\ndef registrar_usuario_e_confirmar(repositorio: RepositorioDeUsuarios, usuario_id: str, nome: str, email: str):\n    repositorio.salvar(usuario_id, nome, email)\n    usuario_salvo = repositorio.buscar(usuario_id)\n    if usuario_salvo:\n        print(f"Usuario registrado com sucesso: {usuario_salvo[\'nome\']} ({usuario_salvo[\'email\']})")\n    else:\n        print("Erro: usuario nao encontrado apos salvar")\n\n\n# Teste com as duas implementacoes\nprint("=== Repositorio em Memoria ===")\nrepo_memoria = RepositorioEmMemoria()\nregistrar_usuario_e_confirmar(repo_memoria, "U001", "Alice", "alice@email.com")\nregistrar_usuario_e_confirmar(repo_memoria, "U002", "Bob", "bob@email.com")\nprint("Todos:", repo_memoria.listar_todos())\nprint("Remover U001:", repo_memoria.remover("U001"))\nprint("Todos apos remocao:", repo_memoria.listar_todos())\n\nprint("\\n=== Repositorio Simulado em Arquivo ===")\nrepo_arquivo = RepositorioSimuladoEmArquivo("usuarios.json")\nregistrar_usuario_e_confirmar(repo_arquivo, "U001", "Alice", "alice@email.com")\n',
      solution:
        'from abc import ABC, abstractmethod\n\n\nclass RepositorioDeUsuarios(ABC):\n    @abstractmethod\n    def salvar(self, usuario_id: str, nome: str, email: str) -> None:\n        pass\n\n    @abstractmethod\n    def buscar(self, usuario_id: str) -> dict | None:\n        pass\n\n    @abstractmethod\n    def listar_todos(self) -> list[dict]:\n        pass\n\n    @abstractmethod\n    def remover(self, usuario_id: str) -> bool:\n        pass\n\n\nclass RepositorioEmMemoria(RepositorioDeUsuarios):\n    def __init__(self):\n        self._dados = {}\n\n    def salvar(self, usuario_id: str, nome: str, email: str) -> None:\n        self._dados[usuario_id] = {"id": usuario_id, "nome": nome, "email": email}\n\n    def buscar(self, usuario_id: str) -> dict | None:\n        return self._dados.get(usuario_id)\n\n    def listar_todos(self) -> list[dict]:\n        return list(self._dados.values())\n\n    def remover(self, usuario_id: str) -> bool:\n        if usuario_id in self._dados:\n            del self._dados[usuario_id]\n            return True\n        return False\n\n\nclass RepositorioSimuladoEmArquivo(RepositorioDeUsuarios):\n    def __init__(self, nome_arquivo: str):\n        self.nome_arquivo = nome_arquivo\n        self._cache = {}\n\n    def salvar(self, usuario_id: str, nome: str, email: str) -> None:\n        self._cache[usuario_id] = {"id": usuario_id, "nome": nome, "email": email}\n        print(f"[Arquivo {self.nome_arquivo}] GRAVAR: {usuario_id} → {nome}, {email}")\n\n    def buscar(self, usuario_id: str) -> dict | None:\n        resultado = self._cache.get(usuario_id)\n        print(f"[Arquivo {self.nome_arquivo}] LER: {usuario_id} → {resultado}")\n        return resultado\n\n    def listar_todos(self) -> list[dict]:\n        print(f"[Arquivo {self.nome_arquivo}] LER TUDO")\n        return list(self._cache.values())\n\n    def remover(self, usuario_id: str) -> bool:\n        if usuario_id in self._cache:\n            del self._cache[usuario_id]\n            print(f"[Arquivo {self.nome_arquivo}] DELETAR: {usuario_id}")\n            return True\n        return False\n\n\ndef registrar_usuario_e_confirmar(repositorio: RepositorioDeUsuarios, usuario_id: str, nome: str, email: str):\n    repositorio.salvar(usuario_id, nome, email)\n    usuario_salvo = repositorio.buscar(usuario_id)\n    if usuario_salvo:\n        print(f"Usuario registrado com sucesso: {usuario_salvo[\'nome\']} ({usuario_salvo[\'email\']})")\n    else:\n        print("Erro: usuario nao encontrado apos salvar")\n\n\nprint("=== Repositorio em Memoria ===")\nrepo_memoria = RepositorioEmMemoria()\nregistrar_usuario_e_confirmar(repo_memoria, "U001", "Alice", "alice@email.com")\nregistrar_usuario_e_confirmar(repo_memoria, "U002", "Bob", "bob@email.com")\nprint("Todos:", repo_memoria.listar_todos())\nprint("Remover U001:", repo_memoria.remover("U001"))\nprint("Todos apos remocao:", repo_memoria.listar_todos())\n\nprint("\\n=== Repositorio Simulado em Arquivo ===")\nrepo_arquivo = RepositorioSimuladoEmArquivo("usuarios.json")\nregistrar_usuario_e_confirmar(repo_arquivo, "U001", "Alice", "alice@email.com")\n',
      hints: [
        'Para implementar RepositorioEmMemoria: self._dados e um dict onde a chave e usuario_id e o valor e outro dict com nome e email.',
        'Para RepositorioSimuladoEmArquivo: use o mesmo self._cache para que buscar() funcione mesmo sem arquivo real. O print simula a operacao de I/O.',
        'A funcao registrar_usuario_e_confirmar recebe RepositorioDeUsuarios como tipo — isso garante que ela funciona com QUALQUER implementacao. Teste ela com os dois repositorios para ver a magica.',
      ],
    },
  ],
};
