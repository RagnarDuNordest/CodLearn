import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'metaprogramacao',
  moduleId: 'python-avancado',
  title: 'Metaprogramacao',
  description: 'Inspecione e modifique objetos em tempo de execucao usando __dunder__ methods, descritores e metaclasses',
  order: 4,
  estimatedMinutes: 30,
  sections: [
    {
      type: 'text',
      content: '## Metaprogramacao: Codigo que Inspeciona Codigo\n\nMetaprogramacao e a capacidade de um programa examinar e modificar sua propria estrutura em tempo de execucao. Python suporta isso amplamente via:\n\n- **Dunder methods** (`__repr__`, `__eq__`, `__len__`, etc.) — personalizacao de comportamento\n- **`__slots__`** — otimizacao de memoria em classes\n- **`__getattr__` / `__setattr__`** — interceptacao de acesso a atributos\n- **Descritores** — objetos que controlam como atributos sao acessados\n- **Metaclasses** — classes que controlam criacao de outras classes\n\n### Dunder Methods Essenciais\n\n```python\nclass Vetor:\n    def __init__(self, x, y): self.x, self.y = x, y\n    def __repr__(self): return f"Vetor({self.x}, {self.y})"\n    def __add__(self, outro): return Vetor(self.x + outro.x, self.y + outro.y)\n    def __len__(self): return 2\n    def __eq__(self, outro): return self.x == outro.x and self.y == outro.y\n    def __hash__(self): return hash((self.x, self.y))\n```',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: 'import time\nfrom functools import wraps\n\n# --- Dunder methods para uma classe de configuracao ---\nclass Config:\n    """Acessa configuracoes como atributos."""\n    \n    def __init__(self, dados: dict):\n        # Usamos object.__setattr__ para evitar recursao no nosso __setattr__\n        object.__setattr__(self, "_dados", dados)\n    \n    def __getattr__(self, nome: str):\n        """Chamado quando atributo nao existe normalmente."""\n        dados = object.__getattribute__(self, "_dados")\n        if nome in dados:\n            return dados[nome]\n        raise AttributeError(f"Config sem a chave \'{nome}\'")\n    \n    def __setattr__(self, nome: str, valor):\n        """Intercepta toda atribuicao de atributo."""\n        dados = object.__getattribute__(self, "_dados")\n        dados[nome] = valor\n    \n    def __contains__(self, item: str) -> bool:\n        """Suporte para: if "chave" in config."""\n        return item in object.__getattribute__(self, "_dados")\n    \n    def __repr__(self) -> str:\n        dados = object.__getattribute__(self, "_dados")\n        return f"Config({dados})"\n\ncfg = Config({"host": "localhost", "porta": 5432})\nprint(cfg.host)      # "localhost"\ncfg.debug = True     # adiciona nova chave\nprint("debug" in cfg)  # True\nprint(cfg)           # Config({...})\n\n# --- Descritor: validacao automatica de atributos ---\nclass PositivoValidado:\n    """Descritor que garante que o valor seja positivo."""\n    \n    def __set_name__(self, owner, name):\n        # name e o nome do atributo na classe (ex: "preco")\n        self._name = f"_{name}"\n    \n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self  # acesso via classe, nao instancia\n        return getattr(obj, self._name, 0)\n    \n    def __set__(self, obj, value):\n        if value < 0:\n            raise ValueError(f"{self._name} deve ser positivo, recebeu {value}")\n        setattr(obj, self._name, value)\n\nclass Produto:\n    preco = PositivoValidado()     # usa o descritor\n    estoque = PositivoValidado()   # usa o descritor\n    \n    def __init__(self, nome: str, preco: float, estoque: int):\n        self.nome = nome\n        self.preco = preco     # chama PositivoValidado.__set__\n        self.estoque = estoque\n\nteclado = Produto("Teclado", 150.0, 10)\nprint(teclado.preco)   # 150.0\ntry:\n    teclado.preco = -50  # ValueError!\nexcept ValueError as e:\n    print(e)  # _preco deve ser positivo, recebeu -50\n\n# --- __init_subclass__: hook para subclasses ---\nclass Plugin:\n    """Registro automatico de todos os plugins."""\n    _plugins: dict = {}\n    \n    def __init_subclass__(cls, nome: str = None, **kwargs):\n        super().__init_subclass__(**kwargs)\n        chave = nome or cls.__name__.lower()\n        Plugin._plugins[chave] = cls\n\nclass PluginCSV(Plugin, nome="csv"):\n    def processar(self, dados): return "csv"\n\nclass PluginJSON(Plugin, nome="json"):\n    def processar(self, dados): return "json"\n\nprint(Plugin._plugins)  # {"csv": PluginCSV, "json": PluginJSON}',
        filename: 'metaprogramacao.py',
        description: '__getattr__/__setattr__ para Config como namespace dinamico. Descritor PositivoValidado reutilizavel para validacao de campos. __init_subclass__ para registro automatico de plugins sem decoradores.',
      },
    },
    {
      type: 'text',
      content: '## __slots__: Otimizacao de Memoria\n\nPor padrao, cada instancia tem um `__dict__` para armazenar atributos. Com muitas instancias, isso consome memoria desnecessaria.\n\n```python\nclass PontoNormal:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n    # Cada instancia tem __dict__ com ~200 bytes de overhead\n\nclass PontoOtimizado:\n    __slots__ = ("x", "y")  # sem __dict__\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n    # ~50 bytes por instancia\n```\n\nUse `__slots__` quando criar milhoes de instancias pequenas (pontos, registros, eventos).\n\n## Inspecao com inspect e getattr\n\n```python\nimport inspect\n\nclass MinhaClasse:\n    def metodo(self): pass\n    atributo = 42\n\n# Listar metodos\nfor nome, obj in inspect.getmembers(MinhaClasse, predicate=inspect.isfunction):\n    print(nome, inspect.signature(obj))\n\n# Acessar atributo dinamicamente\nobj = MinhaClasse()\nnome_atributo = "atributo"\nvalor = getattr(obj, nome_atributo)  # 42\nhasattr(obj, "inexistente")          # False\n```',
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content: 'Metaclasses sao raramente necessarias — a maioria dos casos pode ser resolvida com decoradores de classe, __init_subclass__, ou descritores. Antes de usar uma metaclass, pergunte: isso pode ser um decorador? Na pratica, voce usa metaclasses ao integrar com frameworks (ORM, serializers) mais do que ao construi-los.',
    },
  ],
  challenges: [
    {
      id: 'meta-c1',
      title: 'Implemente um ORM Minimo',
      description: 'Crie um mini ORM usando descritores e __init_subclass__. Cada campo deve ser um descritor Campo que valida o tipo. A classe Model deve registrar automaticamente todas as subclasses. Implemente os metodos save() e to_dict().',
      language: 'python',
      starterCode: 'class Campo:\n    """Descritor que valida o tipo do valor."""\n    \n    def __init__(self, tipo):\n        self.tipo = tipo\n    \n    def __set_name__(self, owner, name):\n        self._name = f"_{name}"\n    \n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        return getattr(obj, self._name, None)\n    \n    def __set__(self, obj, value):\n        # TODO: verificar se value e do tipo correto (isinstance)\n        # TODO: levantar TypeError se nao for\n        setattr(obj, self._name, value)\n\nclass Model:\n    _tabelas = {}  # nome_da_classe -> classe\n    \n    def __init_subclass__(cls, **kwargs):\n        super().__init_subclass__(**kwargs)\n        # TODO: registrar cls em _tabelas\n    \n    def to_dict(self):\n        # TODO: retornar dict com todos os campos e valores\n        # Dica: procure atributos com __set_name__ usando vars(type(self))\n        pass\n    \n    def save(self):\n        # Simulacao: apenas imprime o que seria salvo\n        print(f"INSERT INTO {type(self).__name__} {self.to_dict()}")\n\n# Defina modelos usando a classe Model\nclass Usuario(Model):\n    nome = Campo(str)\n    email = Campo(str)\n    idade = Campo(int)\n\nclass Produto(Model):\n    nome = Campo(str)\n    preco = Campo(float)\n\n# Teste\nu = Usuario()\nu.nome = "Ana"\nu.email = "ana@exemplo.com"\nu.idade = 30\nu.save()  # INSERT INTO Usuario {\'nome\': \'Ana\', ...}\n\nprint(Model._tabelas)  # {\'Usuario\': <class ...>, \'Produto\': ...}\n',
      solution: 'class Campo:\n    def __init__(self, tipo):\n        self.tipo = tipo\n        self._name = None\n    \n    def __set_name__(self, owner, name):\n        self._name = f"_{name}"\n        self.public_name = name\n    \n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        return getattr(obj, self._name, None)\n    \n    def __set__(self, obj, value):\n        if not isinstance(value, self.tipo):\n            raise TypeError(f"Esperado {self.tipo.__name__}, recebeu {type(value).__name__}")\n        setattr(obj, self._name, value)\n\nclass Model:\n    _tabelas = {}\n    \n    def __init_subclass__(cls, **kwargs):\n        super().__init_subclass__(**kwargs)\n        Model._tabelas[cls.__name__] = cls\n    \n    def to_dict(self):\n        resultado = {}\n        for nome, obj in vars(type(self)).items():\n            if isinstance(obj, Campo):\n                resultado[nome] = getattr(self, nome)\n        return resultado\n    \n    def save(self):\n        print(f"INSERT INTO {type(self).__name__} {self.to_dict()}")\n\nclass Usuario(Model):\n    nome = Campo(str)\n    email = Campo(str)\n    idade = Campo(int)\n\nclass Produto(Model):\n    nome = Campo(str)\n    preco = Campo(float)\n\nu = Usuario()\nu.nome = "Ana"\nu.email = "ana@exemplo.com"\nu.idade = 30\nu.save()\n\np = Produto()\np.nome = "Notebook"\np.preco = 2500.0\np.save()\n\ntry:\n    u.idade = "trinta"  # TypeError!\nexcept TypeError as e:\n    print(e)\n\nprint(Model._tabelas)\n',
      hints: [
        'No __set__ do Campo, use isinstance(value, self.tipo) para verificar',
        'Em to_dict, percorra vars(type(self)).items() e filtre por isinstance(obj, Campo)',
        '__init_subclass__ e chamado automaticamente quando uma subclasse e criada — apenas adicione cls a _tabelas',
      ],
    },
  ],
};
