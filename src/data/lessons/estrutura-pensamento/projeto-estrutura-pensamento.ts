import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'projeto-estrutura-pensamento',
  moduleId: 'estrutura-pensamento',
  title: 'Projeto: Sistema de Gerenciamento de Tarefas',
  description:
    'Aplique todos os conceitos do modulo em um projeto real: decomponha o problema, abstraia as partes, desenhe o fluxo e documente o raciocinio para construir um gerenciador de tarefas completo passo a passo.',
  order: 5,
  type: 'lesson',
  estimatedMinutes: 35,
  sections: [
    {
      type: 'text',
      content:
        '## O projeto final: juntando tudo que aprendemos\n\nNeste projeto voce vai construir um **Sistema de Gerenciamento de Tarefas** (task manager) simples em Python. Mas mais importante do que o codigo final, voce vai seguir todo o processo que aprendeu neste modulo:\n\n1. **Pensar como dev** - entender o problema antes de codar\n2. **Decompor** - quebrar o sistema em partes menores\n3. **Abstrair** - criar uma interface clara para cada parte\n4. **Fluxograma** - visualizar o fluxo principal\n5. **Documentar** - pseudocodigo e comentarios antes do codigo\n\n### O problema\n\nVoce foi contratado para criar um gerenciador de tarefas simples. O usuario precisa:\n- Adicionar uma tarefa com titulo e descricao\n- Listar todas as tarefas pendentes\n- Marcar uma tarefa como concluida\n- Ver um resumo (quantas pendentes, quantas concluidas)\n\n### Passo 1: Pensar como dev (Entradas e Saidas)\n\n**Entradas do sistema:**\n- Titulo da tarefa (texto)\n- Descricao da tarefa (texto)\n- ID da tarefa para concluir (numero)\n\n**Saidas do sistema:**\n- Lista de tarefas formatada\n- Confirmacoes de operacoes\n- Resumo de status\n\n**Casos especiais a considerar:**\n- O que acontece se tentar concluir uma tarefa que nao existe?\n- O que acontece se a lista estiver vazia?\n- O que acontece se o titulo for vazio?',
    },
    {
      type: 'text',
      content:
        '## Passo 2: Decomposicao do sistema\n\nUsando o pensamento top-down, vamos decompor:\n\n### Nivel 1 - O sistema inteiro:\n"Sistema de Gerenciamento de Tarefas"\n\n### Nivel 2 - Grandes funcionalidades:\n- Adicionar tarefa\n- Listar tarefas\n- Concluir tarefa\n- Ver resumo\n\n### Nivel 3 - Detalhes de cada funcionalidade:\n\n**Adicionar tarefa:**\n- Validar que o titulo nao esta vazio\n- Gerar um ID unico para a tarefa\n- Criar o objeto tarefa com: id, titulo, descricao, status, data de criacao\n- Adicionar a lista de tarefas\n- Confirmar para o usuario\n\n**Listar tarefas:**\n- Verificar se ha tarefas\n- Para cada tarefa, exibir: id, titulo, status (pendente/concluida)\n- Formatar de forma legivel\n\n**Concluir tarefa:**\n- Receber o ID da tarefa\n- Buscar a tarefa pelo ID\n- Se nao encontrar, informar o erro\n- Se encontrar, mudar status para concluida\n\n**Ver resumo:**\n- Contar tarefas pendentes\n- Contar tarefas concluidas\n- Calcular percentual de conclusao\n- Exibir o relatorio',
    },
    {
      type: 'code',
      codeExample: {
        language: 'bash',
        code: '# FLUXOGRAMA: Operacao "Adicionar Tarefa"\n#\n#          ( INICIO )\n#               |\n#               v\n#    [ Receber titulo e descricao ]\n#               |\n#               v\n#      < titulo esta vazio? >\n#         |           |\n#        SIM          NAO\n#         |           |\n#         v           v\n#  [ Exibir erro  [ Gerar proximo ID ]\n#   e retornar ]       |\n#         |            v\n#         |    [ Criar dicionario da tarefa ]\n#         |    { "id": id, "titulo": titulo,\n#         |      "descricao": descricao,\n#         |      "status": "pendente" }\n#         |            |\n#         |            v\n#         |    [ Adicionar a lista de tarefas ]\n#         |            |\n#         |            v\n#         |    [ Exibir confirmacao ]\n#         |            |\n#         +------+-----+\n#                |\n#                v\n#            ( FIM )',
        filename: 'fluxograma_adicionar_tarefa.txt',
        description:
          'Este fluxograma guia a implementacao da funcao adicionar_tarefa(). Cada bloco retangular vira uma instrucao Python, e o losango vira um if/else.',
      },
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# SISTEMA DE GERENCIAMENTO DE TAREFAS\n# Construido seguindo os 5 passos do modulo:\n# 1. Pensar como dev  2. Decompor  3. Abstrair  4. Fluxograma  5. Documentar\n\n# =====================================================================\n# MODELO DE DADOS (Abstracao)\n# Uma tarefa e representada como um dicionario com estes campos:\n# {\n#   "id": int         -- identificador unico\n#   "titulo": str     -- nome curto da tarefa\n#   "descricao": str  -- detalhes opcionais\n#   "status": str     -- "pendente" ou "concluida"\n# }\n# =====================================================================\n\n# Banco de dados em memoria: lista que armazena todas as tarefas\ntarefas = []\n# Contador para gerar IDs unicos e sequenciais\nproximo_id = 1\n\n\ndef adicionar_tarefa(titulo, descricao=""):\n    """\n    Adiciona uma nova tarefa pendente ao sistema.\n\n    Args:\n        titulo: Texto curto que identifica a tarefa (obrigatorio).\n        descricao: Detalhes adicionais sobre a tarefa (opcional).\n\n    Returns:\n        None. Exibe confirmacao ou mensagem de erro.\n    """\n    global tarefas, proximo_id\n\n    # Validacao: titulo nao pode ser vazio\n    if not titulo or titulo.strip() == "":\n        print("Erro: o titulo da tarefa nao pode ser vazio.")\n        return\n\n    # Criar o objeto tarefa seguindo o modelo de dados definido acima\n    nova_tarefa = {\n        "id": proximo_id,\n        "titulo": titulo.strip(),\n        "descricao": descricao.strip(),\n        "status": "pendente"  # todas as tarefas comecam como pendentes\n    }\n\n    tarefas.append(nova_tarefa)\n    proximo_id = proximo_id + 1  # incrementa para garantir IDs unicos\n    print("Tarefa", nova_tarefa["id"], "adicionada:", nova_tarefa["titulo"])\n\n\ndef listar_tarefas():\n    """\n    Exibe todas as tarefas cadastradas com ID, status e titulo.\n    Se nao houver tarefas, informa o usuario.\n    """\n    if len(tarefas) == 0:\n        print("Nenhuma tarefa cadastrada ainda.")\n        return\n\n    print("\\n=== LISTA DE TAREFAS ===")\n    for tarefa in tarefas:\n        # Simbolo visual para facilitar a leitura: [ ] pendente, [X] concluida\n        simbolo = "[X]" if tarefa["status"] == "concluida" else "[ ]"\n        print(simbolo, "ID", tarefa["id"], "-", tarefa["titulo"])\n        if tarefa["descricao"]:  # exibe descricao apenas se nao estiver vazia\n            print("    Detalhes:", tarefa["descricao"])\n    print("========================")\n\n\ndef concluir_tarefa(id_tarefa):\n    """\n    Marca uma tarefa especifica como concluida.\n\n    Args:\n        id_tarefa: O ID numerico da tarefa a ser concluida.\n\n    Returns:\n        None. Exibe confirmacao ou erro se ID nao encontrado.\n    """\n    # Buscar a tarefa pelo ID na lista\n    tarefa_encontrada = None\n    for tarefa in tarefas:\n        if tarefa["id"] == id_tarefa:\n            tarefa_encontrada = tarefa\n            break  # para o loop ao encontrar\n\n    # Tratar caso de tarefa nao encontrada\n    if tarefa_encontrada is None:\n        print("Erro: tarefa com ID", id_tarefa, "nao encontrada.")\n        return\n\n    # Tratar caso de tarefa ja concluida (evita mudanca desnecessaria)\n    if tarefa_encontrada["status"] == "concluida":\n        print("Aviso: a tarefa", id_tarefa, "ja estava concluida.")\n        return\n\n    tarefa_encontrada["status"] = "concluida"\n    print("Tarefa", id_tarefa, "marcada como concluida:", tarefa_encontrada["titulo"])\n\n\ndef ver_resumo():\n    """\n    Exibe um relatorio de progresso com contagem de tarefas por status.\n    """\n    total = len(tarefas)\n    if total == 0:\n        print("Nenhuma tarefa para resumir.")\n        return\n\n    # Contar tarefas por status\n    concluidas = 0\n    for tarefa in tarefas:\n        if tarefa["status"] == "concluida":\n            concluidas = concluidas + 1\n    pendentes = total - concluidas\n\n    # Calcular percentual de conclusao\n    percentual = (concluidas / total) * 100\n\n    print("\\n=== RESUMO ===")\n    print("Total de tarefas:", total)\n    print("Pendentes:       ", pendentes)\n    print("Concluidas:      ", concluidas)\n    print("Progresso:       ", round(percentual, 1), "%")\n    print("==============")\n\n\n# =====================================================================\n# DEMONSTRACAO DO SISTEMA\n# =====================================================================\nprint("--- Adicionando tarefas ---")\nadicionar_tarefa("Estudar Python", "Completar o modulo de logica")\nadicionar_tarefa("Fazer exercicios", "Resolver os desafios do CodLearn")\nadicionar_tarefa("Revisar conceitos")\nadicionar_tarefa("")  # deve mostrar erro\n\nlistar_tarefas()\n\nprint("\\n--- Concluindo tarefas ---")\nconcluir_tarefa(1)\nconcluir_tarefa(1)   # ja esta concluida\nconcluir_tarefa(99)  # nao existe\n\nlistar_tarefas()\nver_resumo()',
        filename: 'gerenciador_tarefas.py',
        description:
          'O sistema completo aplicando todos os conceitos do modulo. Observe: decomposicao em 4 funcoes, abstracao do modelo de dados, fluxogramas guiando cada funcao, e documentacao com docstrings e comentarios explicando o porque de cada decisao.',
      },
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'Antes de tentar os desafios, execute mentalmente o codigo acima linha por linha. Pergunte: por que o ID e gerado com uma variavel global? Por que usamos "break" no loop de busca? Por que verificamos se a tarefa ja estava concluida? Cada escolha de implementacao foi documentada -- tentar entender o "por que" antes do "como" e exatamente o pensamento de desenvolvedor que este modulo ensinou.',
    },
  ],
  challenges: [
    {
      id: 'projeto-ep-c1',
      title: 'Implementando o Gerenciador de Tarefas',
      description:
        'Implemente o sistema de gerenciamento de tarefas do zero, seguindo os 5 passos do modulo. Voce deve ter as funcoes: adicionar_tarefa(titulo, descricao), listar_tarefas(), concluir_tarefa(id), e ver_resumo(). Siga a estrutura apresentada na licao, mas escreva voce mesmo cada funcao.',
      language: 'python',
      starterCode:
        '# PROJETO: Sistema de Gerenciamento de Tarefas\n#\n# DECOMPOSICAO (ja feita para voce):\n# - adicionar_tarefa(titulo, descricao): adiciona nova tarefa\n# - listar_tarefas(): exibe todas as tarefas\n# - concluir_tarefa(id): marca como concluida\n# - ver_resumo(): mostra estatisticas\n#\n# MODELO DE DADOS (abstracao):\n# tarefa = {"id": int, "titulo": str, "descricao": str, "status": str}\n\ntarefas = []\nproximo_id = 1\n\ndef adicionar_tarefa(titulo, descricao=""):\n    # Validar titulo, criar dicionario, adicionar a lista\n    pass\n\ndef listar_tarefas():\n    # Se vazia, informar. Senao, exibir cada tarefa com ID e status\n    pass\n\ndef concluir_tarefa(id_tarefa):\n    # Buscar por ID, tratar nao encontrado, mudar status\n    pass\n\ndef ver_resumo():\n    # Contar pendentes e concluidas, calcular percentual\n    pass\n\n# Teste seu sistema:\nadicionar_tarefa("Aprender decomposicao")\nadicionar_tarefa("Estudar abstracao", "Ler a licao 3 do modulo")\nadicionar_tarefa("Praticar fluxogramas")\nlistar_tarefas()\nconcluir_tarefa(1)\nconcluir_tarefa(2)\nver_resumo()\n',
      solution:
        'tarefas = []\nproximo_id = 1\n\ndef adicionar_tarefa(titulo, descricao=""):\n    global tarefas, proximo_id\n    if not titulo or titulo.strip() == "":\n        print("Erro: titulo nao pode ser vazio.")\n        return\n    nova = {\n        "id": proximo_id,\n        "titulo": titulo.strip(),\n        "descricao": descricao.strip(),\n        "status": "pendente"\n    }\n    tarefas.append(nova)\n    proximo_id = proximo_id + 1\n    print("Tarefa", nova["id"], "adicionada:", nova["titulo"])\n\ndef listar_tarefas():\n    if len(tarefas) == 0:\n        print("Nenhuma tarefa cadastrada.")\n        return\n    print("\\n=== TAREFAS ===")\n    for t in tarefas:\n        s = "[X]" if t["status"] == "concluida" else "[ ]"\n        print(s, "ID", t["id"], "-", t["titulo"])\n    print("===============")\n\ndef concluir_tarefa(id_tarefa):\n    encontrada = None\n    for t in tarefas:\n        if t["id"] == id_tarefa:\n            encontrada = t\n            break\n    if encontrada is None:\n        print("Erro: tarefa", id_tarefa, "nao encontrada.")\n        return\n    if encontrada["status"] == "concluida":\n        print("Tarefa", id_tarefa, "ja estava concluida.")\n        return\n    encontrada["status"] = "concluida"\n    print("Tarefa", id_tarefa, "concluida:", encontrada["titulo"])\n\ndef ver_resumo():\n    total = len(tarefas)\n    if total == 0:\n        print("Sem tarefas.")\n        return\n    concluidas = sum(1 for t in tarefas if t["status"] == "concluida")\n    pendentes = total - concluidas\n    pct = round((concluidas / total) * 100, 1)\n    print("\\n=== RESUMO ===")\n    print("Total:", total, "| Pendentes:", pendentes, "| Concluidas:", concluidas)\n    print("Progresso:", pct, "%")\n\nadicionar_tarefa("Aprender decomposicao")\nadicionar_tarefa("Estudar abstracao", "Ler a licao 3 do modulo")\nadicionar_tarefa("Praticar fluxogramas")\nlistar_tarefas()\nconcluir_tarefa(1)\nconcluir_tarefa(2)\nver_resumo()',
      hints: [
        'Em "adicionar_tarefa", use "global tarefas, proximo_id" para modificar as variaveis globais. Crie o dicionario com os 4 campos e use tarefas.append() para adicionar.',
        'Em "concluir_tarefa", percorra a lista com "for t in tarefas: if t[\'id\'] == id_tarefa:" para encontrar a tarefa. Use uma variavel auxiliar "encontrada = None" e atribua a tarefa quando encontrada.',
        'Em "ver_resumo", use um loop para contar concluidas: "for t in tarefas: if t[\'status\'] == \'concluida\': concluidas += 1". O percentual e (concluidas / total) * 100.',
      ],
    },
    {
      id: 'projeto-ep-c2',
      title: 'Extendendo com Sistema de Prioridades',
      description:
        'Expanda o gerenciador de tarefas adicionando um campo de prioridade. Prioridade pode ser "alta", "media" ou "baixa". Modifique adicionar_tarefa para aceitar prioridade (padrao "media"). Crie a funcao listar_por_prioridade(prioridade) que lista apenas tarefas de uma prioridade especifica. E modifique ver_resumo para mostrar quantas tarefas existem por prioridade.',
      language: 'python',
      starterCode:
        '# Gerenciador de Tarefas com Prioridades\n# Expanda o sistema anterior com o campo "prioridade"\n#\n# MUDANCAS NECESSARIAS (decomposicao das alteracoes):\n# 1. Adicionar campo "prioridade" ao modelo de dados\n# 2. Modificar adicionar_tarefa() para aceitar prioridade\n# 3. Criar listar_por_prioridade(prioridade)\n# 4. Atualizar ver_resumo() para contar por prioridade\n# 5. Atualizar listar_tarefas() para mostrar a prioridade\n\ntarefas = []\nproximo_id = 1\n\nPRIORIDADES_VALIDAS = ["alta", "media", "baixa"]\n\ndef adicionar_tarefa(titulo, descricao="", prioridade="media"):\n    # Valide a prioridade (deve estar em PRIORIDADES_VALIDAS)\n    # Adicione o campo prioridade ao dicionario da tarefa\n    pass\n\ndef listar_tarefas():\n    # Exiba as tarefas com o campo prioridade incluido\n    pass\n\ndef concluir_tarefa(id_tarefa):\n    # Mesma logica anterior\n    pass\n\ndef listar_por_prioridade(prioridade):\n    # Exibe apenas as tarefas com a prioridade informada\n    pass\n\ndef ver_resumo():\n    # Mostre contagem total, por status E por prioridade\n    pass\n\n# Teste:\nadicionar_tarefa("Entregar relatorio", prioridade="alta")\nadicionar_tarefa("Responder emails", prioridade="media")\nadicionar_tarefa("Organizar desktop", prioridade="baixa")\nadicionar_tarefa("Corrigir bug critico", prioridade="alta")\nadicionar_tarefa("Tarefa invalida", prioridade="urgente")  # prioridade invalida\n\nlistar_tarefas()\nlistar_por_prioridade("alta")\nconcluir_tarefa(1)\nver_resumo()\n',
      solution:
        'tarefas = []\nproximo_id = 1\n\nPRIORIDADES_VALIDAS = ["alta", "media", "baixa"]\n\ndef adicionar_tarefa(titulo, descricao="", prioridade="media"):\n    global tarefas, proximo_id\n    if not titulo or titulo.strip() == "":\n        print("Erro: titulo nao pode ser vazio.")\n        return\n    if prioridade not in PRIORIDADES_VALIDAS:\n        print("Erro: prioridade invalida. Use: alta, media ou baixa.")\n        return\n    nova = {\n        "id": proximo_id,\n        "titulo": titulo.strip(),\n        "descricao": descricao.strip(),\n        "status": "pendente",\n        "prioridade": prioridade\n    }\n    tarefas.append(nova)\n    proximo_id = proximo_id + 1\n    print("Tarefa", nova["id"], "adicionada [prioridade", prioridade + "]:", nova["titulo"])\n\ndef listar_tarefas():\n    if len(tarefas) == 0:\n        print("Nenhuma tarefa cadastrada.")\n        return\n    print("\\n=== TAREFAS ===")\n    for t in tarefas:\n        s = "[X]" if t["status"] == "concluida" else "[ ]"\n        print(s, "[" + t["prioridade"] + "]", "ID", t["id"], "-", t["titulo"])\n    print("===============")\n\ndef concluir_tarefa(id_tarefa):\n    encontrada = None\n    for t in tarefas:\n        if t["id"] == id_tarefa:\n            encontrada = t\n            break\n    if encontrada is None:\n        print("Erro: tarefa", id_tarefa, "nao encontrada.")\n        return\n    if encontrada["status"] == "concluida":\n        print("Tarefa", id_tarefa, "ja estava concluida.")\n        return\n    encontrada["status"] = "concluida"\n    print("Tarefa", id_tarefa, "concluida:", encontrada["titulo"])\n\ndef listar_por_prioridade(prioridade):\n    if prioridade not in PRIORIDADES_VALIDAS:\n        print("Prioridade invalida. Use: alta, media ou baixa.")\n        return\n    filtradas = [t for t in tarefas if t["prioridade"] == prioridade]\n    if len(filtradas) == 0:\n        print("Nenhuma tarefa com prioridade", prioridade + ".")\n        return\n    print("\\n=== TAREFAS -", prioridade.upper(), "===")\n    for t in filtradas:\n        s = "[X]" if t["status"] == "concluida" else "[ ]"\n        print(s, "ID", t["id"], "-", t["titulo"])\n\ndef ver_resumo():\n    total = len(tarefas)\n    if total == 0:\n        print("Sem tarefas.")\n        return\n    concluidas = sum(1 for t in tarefas if t["status"] == "concluida")\n    pendentes = total - concluidas\n    pct = round((concluidas / total) * 100, 1)\n    altas = sum(1 for t in tarefas if t["prioridade"] == "alta")\n    medias = sum(1 for t in tarefas if t["prioridade"] == "media")\n    baixas = sum(1 for t in tarefas if t["prioridade"] == "baixa")\n    print("\\n=== RESUMO ===")\n    print("Total:", total, "| Pendentes:", pendentes, "| Concluidas:", concluidas)\n    print("Progresso:", pct, "%")\n    print("Por prioridade - Alta:", altas, "| Media:", medias, "| Baixa:", baixas)\n\nadicionar_tarefa("Entregar relatorio", prioridade="alta")\nadicionar_tarefa("Responder emails", prioridade="media")\nadicionar_tarefa("Organizar desktop", prioridade="baixa")\nadicionar_tarefa("Corrigir bug critico", prioridade="alta")\nadicionar_tarefa("Tarefa invalida", prioridade="urgente")\n\nlistar_tarefas()\nlistar_por_prioridade("alta")\nconcluir_tarefa(1)\nver_resumo()',
      hints: [
        'Em "adicionar_tarefa", adicione "if prioridade not in PRIORIDADES_VALIDAS:" para validar e adicione "prioridade": prioridade ao dicionario da tarefa.',
        'Em "listar_por_prioridade", crie uma lista filtrada: "filtradas = []" e use um loop para adicionar tarefas cuja prioridade bata com o parametro.',
        'Em "ver_resumo", conte por prioridade com loops separados ou com sum(): "altas = sum(1 for t in tarefas if t[\'prioridade\'] == \'alta\')".',
      ],
    },
  ],
};
