import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'rest-e-design-de-apis',
  moduleId: 'redes',
  title: 'REST e Design de APIs',
  description: 'Principios REST, boas praticas de URLs, versionamento, autenticacao, paginacao e como projetar APIs que desenvolvedores amam usar',
  order: 2,
  estimatedMinutes: 35,
  sections: [
    {
      type: 'text',
      content:
        '## REST e Design de APIs\n\nREST (Representational State Transfer) nao e um protocolo — e um conjunto de restricoes arquiteturais definido por Roy Fielding em 2000. APIs que seguem essas restricoes sao chamadas "RESTful".\n\n### Os 6 Principios REST\n\n**(1) Stateless (Sem Estado)**\nCada requisicao deve conter TUDO que o servidor precisa para processa-la. O servidor nao guarda estado de sessao entre requests. Isso torna o sistema horizontalmente escalavel — qualquer servidor do pool pode atender qualquer request.\n\n**(2) Client-Server**\nSeparacao clara entre cliente (UI, mobile) e servidor (dados, logica). Cada lado pode evoluir independentemente.\n\n**(3) Cacheable**\nRespostas devem indicar se podem ser cacheadas. Reduz carga no servidor e latencia para o cliente.\n\n**(4) Interface Uniforme**\nA mesma convencao em toda a API: URLs representam recursos, metodos HTTP representam acoes.\n\n**(5) Sistema em Camadas**\nO cliente nao sabe se fala com o servidor final, um balanceador de carga ou um proxy de cache.\n\n**(6) Code on Demand (opcional)**\nServidor pode enviar codigo executavel ao cliente (ex: JavaScript).\n\n---\n\n### Boas Praticas de URLs\n\n**Recursos sao substantivos, metodos HTTP sao verbos:**\n```\n# Bom\nGET    /usuarios\nGET    /usuarios/42\nPOST   /usuarios\nPUT    /usuarios/42\nDELETE /usuarios/42\n\n# Ruim — verbo na URL\nGET  /buscar-usuarios\nPOST /criar-usuario\nPOST /deletar-usuario/42\n```\n\n**Hierarquias de recursos:**\n```\n# Recursos aninhados (pertencimento)\nGET  /usuarios/42/pedidos          # pedidos do usuario 42\nGET  /usuarios/42/pedidos/7        # pedido 7 do usuario 42\nPOST /usuarios/42/pedidos          # criar pedido para usuario 42\n\n# Cuidado: nao aninhe mais de 2 niveis — fica confuso\n# Ruim:\nGET /usuarios/42/pedidos/7/itens/3/avaliacoes\n```\n\n**Use plural consistentemente:**\n```\n# Consistente\nGET /usuarios\nGET /usuarios/1\nGET /produtos\n\n# Inconsistente — evite\nGET /usuario\nGET /usuarios/1\nGET /produto\n```\n\n**Filtros, ordenacao e busca via query string:**\n```\nGET /produtos?categoria=eletronicos\nGET /produtos?preco_min=100&preco_max=500\nGET /usuarios?ordem=nome&direcao=asc\nGET /produtos?q=notebook\n```\n\n---\n\n### Versionamento de API\n\nAPIs mudam. Clientes existentes nao podem quebrar. Estrategias:\n\n**Via URL (mais comum, mais explicito):**\n```\n/v1/usuarios\n/v2/usuarios\n```\n\n**Via header (mais clean, menos visivel):**\n```\nAccept: application/vnd.minhaapi.v2+json\n```\n\n**Via query param (menos recomendado):**\n```\n/usuarios?version=2\n```\n\nRecomendacao: versione via URL. E simples, testavel no browser e visivel em logs.\n\n---\n\n### Autenticacao\n\n**API Keys** — simples, para servico-a-servico:\n```\nGET /dados\nX-API-Key: abc123xyz\n# ou\nGET /dados?api_key=abc123xyz\n```\n\n**Bearer Tokens (JWT)** — para usuarios autenticados:\n```\nGET /perfil\nAuthorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\n```\nO JWT contem claims (dados) assinados. O servidor valida a assinatura sem consultar banco de dados.\n\n**OAuth 2.0** — para acesso delegado (login com Google):\n- Cliente obtem authorization code\n- Troca por access token\n- Usa access token nas requisicoes\n\n---\n\n### Paginacao\n\n**Offset/Limit (mais simples):**\n```\nGET /produtos?limite=20&offset=40  # pagina 3\n```\nProblema: se um item e inserido enquanto o usuario pagina, ele pode ser pulado ou aparecer duplicado.\n\n**Cursor-based (mais robusto):**\n```\nGET /posts?limite=20\n# Resposta inclui:\n{\n  "dados": [...],\n  "cursor_proximo": "eyJpZCI6MjB9",\n  "tem_proxima": true\n}\nGET /posts?limite=20&cursor=eyJpZCI6MjB9\n```\nO cursor e um ponteiro opaco para o ultimo item visto. Mais estavel para feeds em tempo real.',
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'Regra de ouro para status codes em REST: 200 para GET/PUT/PATCH com sucesso; 201 para POST que cria recurso (inclua o header Location: /recursos/42); 204 para DELETE com sucesso; 404 quando o ID nao existe; 422 quando os dados sao invalidos semanticamente (campo obrigatorio ausente, email malformado, etc.).',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# Exemplos de design REST com Flask (demonstracao conceitual)\n# Para rodar: pip install flask\n\nfrom flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n# Dados em memoria para demonstracao\nlivros = {\n    1: {"id": 1, "titulo": "Clean Code", "autor": "Robert Martin", "disponivel": True},\n    2: {"id": 2, "titulo": "The Pragmatic Programmer", "autor": "Hunt & Thomas", "disponivel": True},\n}\nproximo_id = 3\n\n\n# GET /v1/livros — lista todos com paginacao e filtro\n@app.route("/v1/livros", methods=["GET"])\ndef listar_livros():\n    limite = int(request.args.get("limite", 10))\n    offset = int(request.args.get("offset", 0))\n    disponivel = request.args.get("disponivel")\n\n    todos = list(livros.values())\n\n    if disponivel is not None:\n        disponivel_bool = disponivel.lower() == "true"\n        todos = [l for l in todos if l["disponivel"] == disponivel_bool]\n\n    total = len(todos)\n    pagina = todos[offset:offset + limite]\n\n    return jsonify({\n        "dados": pagina,\n        "total": total,\n        "limite": limite,\n        "offset": offset,\n        "tem_proxima": offset + limite < total\n    }), 200\n\n\n# GET /v1/livros/<id> — busca livro especifico\n@app.route("/v1/livros/<int:livro_id>", methods=["GET"])\ndef buscar_livro(livro_id):\n    livro = livros.get(livro_id)\n    if not livro:\n        return jsonify({"erro": "Livro nao encontrado"}), 404\n    return jsonify(livro), 200\n\n\n# POST /v1/livros — cria novo livro\n@app.route("/v1/livros", methods=["POST"])\ndef criar_livro():\n    global proximo_id\n    dados = request.get_json()\n\n    # Validacao\n    if not dados:\n        return jsonify({"erro": "Body obrigatorio"}), 400\n    if "titulo" not in dados:\n        return jsonify({"erro": "Campo titulo e obrigatorio"}), 422\n    if "autor" not in dados:\n        return jsonify({"erro": "Campo autor e obrigatorio"}), 422\n\n    novo_livro = {\n        "id": proximo_id,\n        "titulo": dados["titulo"],\n        "autor": dados["autor"],\n        "disponivel": dados.get("disponivel", True)\n    }\n    livros[proximo_id] = novo_livro\n    proximo_id += 1\n\n    resposta = jsonify(novo_livro)\n    resposta.headers["Location"] = f"/v1/livros/{novo_livro[\'id\']}"\n    return resposta, 201\n\n\n# PATCH /v1/livros/<id> — atualiza campos especificos\n@app.route("/v1/livros/<int:livro_id>", methods=["PATCH"])\ndef atualizar_livro(livro_id):\n    livro = livros.get(livro_id)\n    if not livro:\n        return jsonify({"erro": "Livro nao encontrado"}), 404\n\n    dados = request.get_json() or {}\n    campos_atualizaveis = {"titulo", "autor", "disponivel"}\n\n    for campo, valor in dados.items():\n        if campo in campos_atualizaveis:\n            livro[campo] = valor\n\n    return jsonify(livro), 200\n\n\n# DELETE /v1/livros/<id>\n@app.route("/v1/livros/<int:livro_id>", methods=["DELETE"])\ndef deletar_livro(livro_id):\n    if livro_id not in livros:\n        return jsonify({"erro": "Livro nao encontrado"}), 404\n    del livros[livro_id]\n    return "", 204\n\n\nif __name__ == "__main__":\n    app.run(debug=True, port=5000)',
        filename: 'api_biblioteca_flask.py',
        description:
          'API REST completa para um sistema de biblioteca usando Flask. Demonstra versionamento via URL (/v1/), paginacao com offset/limit, status codes corretos (201 com Location, 204 sem body, 422 para validacao) e filtros via query string.',
      },
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: 'import requests\nimport json\n\n# Cliente para testar a API de biblioteca\nBASE = "http://localhost:5000/v1"\n\ndef testar_api_biblioteca():\n    """Testa todos os endpoints da API de livros."""\n\n    # 1. Listar livros\n    print("=== Listando livros ===")\n    resp = requests.get(f"{BASE}/livros?limite=5&offset=0")\n    print(f"Status: {resp.status_code}")\n    dados = resp.json()\n    print(f"Total: {dados[\'total\']}, Tem proxima pagina: {dados[\'tem_proxima\']}")\n    for livro in dados[\'dados\']:\n        print(f"  [{livro[\'id\']}] {livro[\'titulo\']} - {livro[\'autor\']}")\n\n    # 2. Criar livro\n    print("\\n=== Criando livro ===")\n    resp = requests.post(\n        f"{BASE}/livros",\n        json={"titulo": "Domain-Driven Design", "autor": "Eric Evans"},\n        headers={"Authorization": "Bearer meu-token"}\n    )\n    print(f"Status: {resp.status_code}")  # 201\n    print(f"Location: {resp.headers.get(\'Location\')}")\n    novo_livro = resp.json()\n    novo_id = novo_livro["id"]\n\n    # 3. Buscar livro criado\n    print(f"\\n=== Buscando livro {novo_id} ===")\n    resp = requests.get(f"{BASE}/livros/{novo_id}")\n    print(f"Status: {resp.status_code}")\n    print(f"Livro: {resp.json()}")\n\n    # 4. Atualizar disponibilidade\n    print(f"\\n=== Atualizando livro {novo_id} ===")\n    resp = requests.patch(f"{BASE}/livros/{novo_id}", json={"disponivel": False})\n    print(f"Status: {resp.status_code}")\n    print(f"Livro atualizado: {resp.json()}")\n\n    # 5. Deletar livro\n    print(f"\\n=== Deletando livro {novo_id} ===")\n    resp = requests.delete(f"{BASE}/livros/{novo_id}")\n    print(f"Status: {resp.status_code}")  # 204\n\n    # 6. Verificar 404\n    print(f"\\n=== Buscando livro deletado (deve dar 404) ===")\n    resp = requests.get(f"{BASE}/livros/{novo_id}")\n    print(f"Status: {resp.status_code}")  # 404\n    print(f"Erro: {resp.json()}")\n\n\nif __name__ == "__main__":\n    testar_api_biblioteca()',
        filename: 'cliente_biblioteca.py',
        description:
          'Cliente Python que testa o ciclo completo CRUD da API de biblioteca: listagem com paginacao, criacao com verificacao do header Location, leitura, atualizacao parcial e delecao com verificacao de 404.',
      },
    },
    {
      type: 'callout',
      calloutType: 'warning',
      content:
        'Nunca exponha detalhes internos de implementacao na API: IDs sequenciais revelam quantos registros voce tem (use UUIDs), mensagens de erro nunca devem incluir stack traces ou queries SQL, e headers como X-Powered-By: Express ou Server: Apache revelam sua stack para atacantes.',
    },
  ],
  challenges: [
    {
      id: 'redes-c3',
      title: 'Projetar Endpoints REST para um Sistema de Biblioteca',
      description:
        'Implemente a funcao `validar_design_rest(endpoints)` que recebe uma lista de dicionarios com chaves "metodo" e "url", e retorna uma lista de problemas encontrados. Verifique: (1) verbos na URL (/buscar, /criar, /deletar), (2) ausencia de versionamento (/v1/ ou /v2/), (3) uso incorreto de metodo (DELETE com /criar na URL, etc), (4) URLs com mais de 3 segmentos aninhados. Retorne lista vazia se tudo estiver correto.',
      language: 'python',
      starterCode:
        '# Validador de design REST\ndef validar_design_rest(endpoints):\n    """\n    Valida uma lista de endpoints REST e retorna problemas encontrados.\n    \n    Args:\n        endpoints: lista de dicts com "metodo" (str) e "url" (str)\n    \n    Returns:\n        lista de strings descrevendo os problemas encontrados\n    """\n    problemas = []\n    # Seu codigo aqui\n    return problemas\n\n\n# Teste com endpoints ruins\nendpoints_ruins = [\n    {"metodo": "GET",    "url": "/buscar-usuarios"},\n    {"metodo": "POST",   "url": "/criar-produto"},\n    {"metodo": "GET",    "url": "/usuarios"},            # Sem versao\n    {"metodo": "DELETE", "url": "/v1/posts/1/comentarios/2/replies/3/likes"},\n    {"metodo": "GET",    "url": "/v1/produtos"},         # OK\n]\n\nproblemas = validar_design_rest(endpoints_ruins)\nprint(f"Problemas encontrados: {len(problemas)}")\nfor p in problemas:\n    print(f"  - {p}")\n\n# Deve retornar lista vazia\nendpoints_ok = [\n    {"metodo": "GET",    "url": "/v1/usuarios"},\n    {"metodo": "POST",   "url": "/v1/usuarios"},\n    {"metodo": "GET",    "url": "/v1/usuarios/42"},\n    {"metodo": "DELETE", "url": "/v1/usuarios/42"},\n    {"metodo": "GET",    "url": "/v1/usuarios/42/pedidos"},\n]\nprint(f"\\nEndpoints ok, problemas: {validar_design_rest(endpoints_ok)}")\n',
      solution:
        'def validar_design_rest(endpoints):\n    problemas = []\n    verbos_ruins = ["buscar", "criar", "deletar", "atualizar", "listar", "get", "post", "delete", "update", "fetch"]\n\n    for ep in endpoints:\n        metodo = ep["metodo"].upper()\n        url = ep["url"].lower()\n        segmentos = [s for s in url.split("/") if s]\n\n        # Verifica verbos na URL\n        for verbo in verbos_ruins:\n            for segmento in segmentos:\n                # Remove parametros como :id ou {id}\n                seg_limpo = segmento.replace("-", "_").split("{")[0].split(":")[0]\n                if seg_limpo == verbo or seg_limpo.startswith(verbo + "_"):\n                    problemas.append(\n                        f"{metodo} {ep[\'url\']}: verbo \'{verbo}\' encontrado na URL — use substantivos e metodos HTTP"\n                    )\n                    break\n\n        # Verifica versionamento\n        tem_versao = any(s.startswith("v") and s[1:].isdigit() for s in segmentos)\n        if not tem_versao:\n            problemas.append(\n                f"{metodo} {ep[\'url\']}: URL sem versionamento — adicione /v1/, /v2/, etc."\n            )\n\n        # Verifica profundidade de aninhamento (mais de 3 recursos)\n        segmentos_sem_versao = [s for s in segmentos if not (s.startswith("v") and s[1:].isdigit())]\n        recursos = [s for s in segmentos_sem_versao if not s.isdigit() and not s.startswith("{")]\n        if len(recursos) > 3:\n            problemas.append(\n                f"{metodo} {ep[\'url\']}: URL com {len(recursos)} niveis de aninhamento — limite a 2-3"\n            )\n\n    return problemas\n\n\nendpoints_ruins = [\n    {"metodo": "GET",    "url": "/buscar-usuarios"},\n    {"metodo": "POST",   "url": "/criar-produto"},\n    {"metodo": "GET",    "url": "/usuarios"},\n    {"metodo": "DELETE", "url": "/v1/posts/1/comentarios/2/replies/3/likes"},\n    {"metodo": "GET",    "url": "/v1/produtos"},\n]\n\nproblemas = validar_design_rest(endpoints_ruins)\nprint(f"Problemas encontrados: {len(problemas)}")\nfor p in problemas:\n    print(f"  - {p}")\n\nendpoints_ok = [\n    {"metodo": "GET",    "url": "/v1/usuarios"},\n    {"metodo": "POST",   "url": "/v1/usuarios"},\n    {"metodo": "GET",    "url": "/v1/usuarios/42"},\n    {"metodo": "DELETE", "url": "/v1/usuarios/42"},\n    {"metodo": "GET",    "url": "/v1/usuarios/42/pedidos"},\n]\nprint(f"\\nEndpoints ok, problemas: {validar_design_rest(endpoints_ok)}")\n',
      hints: [
        'Faca split("/") na URL e filtre segmentos vazios. Verifique cada segmento contra uma lista de verbos proibidos',
        'Para detectar versao, procure segmentos que comecam com "v" seguido de numero: s.startswith("v") and s[1:].isdigit()',
        'Para contar profundidade, ignore numeros e IDs entre chaves — eles sao parametros, nao niveis de recurso',
      ],
    },
  ],
};
