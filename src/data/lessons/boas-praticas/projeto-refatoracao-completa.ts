import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'projeto-refatoracao-completa',
  moduleId: 'boas-praticas',
  title: 'Projeto: Refatoracao Completa',
  description: 'Refatore um sistema de biblioteca completo aplicando nomenclatura, SRP, DRY, early return e padroes de design',
  order: 5,
  estimatedMinutes: 60,
  sections: [
    {
      type: 'text',
      content:
        '## Projeto Final: Refatorando um Sistema Real\n\nNeste projeto voce vai aplicar **tudo que aprendeu** neste modulo para transformar um codigo de baixa qualidade em codigo limpo, organizado e profissional.\n\nVoce vai trabalhar com um sistema de biblioteca que tem os seguintes problemas:\n\n1. **Nomenclatura ruim** — variaveis e funcoes com nomes opacos\n2. **Funcoes "deus"** — funcoes que fazem muitas coisas\n3. **Codigo duplicado** — logica repetida em varios lugares\n4. **Numeros magicos** — valores numericos sem contexto\n5. **Indentacao excessiva** — sem early return\n6. **Zero organizacao** — tudo em um arquivo gigante\n\nSua missao: refatorar o sistema aplicando as boas praticas do modulo. O comportamento deve ser identico — apenas a qualidade do codigo muda.\n\n### Criterios de avaliacao\n\n- Todos os nomes descrevem o proposito (nao o tipo)\n- Nenhuma funcao tem mais de uma responsabilidade\n- Zero codigo duplicado (principio DRY)\n- Constantes nomeadas para todos os valores "magicos"\n- Early return nos lugares adequados\n- Estrutura clara e navegavel',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# SISTEMA DE BIBLIOTECA — ANTES DA REFATORACAO\n# Este codigo funciona, mas viola todas as boas praticas\n\nlivros = []\nusuarios = []\nemprestimos = []\n\ndef proc(n, a, pg, q):\n    if n != None:\n        if a != None:\n            if pg != None and pg > 0:\n                if q != None and q > 0:\n                    l = {"titulo": n, "autor": a, "paginas": pg, "qtd": q, "disponivel": q}\n                    livros.append(l)\n                    return True\n    return False\n\ndef pegar(uid, tid):\n    u = None\n    for x in usuarios:\n        if x["id"] == uid:\n            u = x\n    if u == None:\n        return "erro: usuario nao encontrado"\n\n    t = None\n    for x in livros:\n        if x["titulo"] == tid:\n            t = x\n    if t == None:\n        return "erro: livro nao encontrado"\n\n    cnt = 0\n    for e in emprestimos:\n        if e["uid"] == uid and e["ativo"] == True:\n            cnt = cnt + 1\n    if cnt >= 3:\n        return "erro: limite de emprestimos atingido"\n\n    if t["disponivel"] <= 0:\n        return "erro: sem estoque"\n\n    t["disponivel"] = t["disponivel"] - 1\n    emprestimos.append({"uid": uid, "titulo": tid, "ativo": True, "dias": 0})\n    return "ok"\n\ndef dev(uid, tid):\n    e = None\n    for x in emprestimos:\n        if x["uid"] == uid and x["titulo"] == tid and x["ativo"] == True:\n            e = x\n    if e == None:\n        return "erro"\n    e["ativo"] = False\n    for x in livros:\n        if x["titulo"] == tid:\n            x["disponivel"] = x["disponivel"] + 1\n    mult = 0\n    if e["dias"] > 14:\n        mult = (e["dias"] - 14) * 0.5\n    return mult',
        filename: 'biblioteca_antes.py',
        description:
          'Este e o sistema de biblioteca original. Funciona corretamente, mas e dificil de ler, manter e expandir. Nomes como "proc", "pegar", "dev", "uid", "tid", "x", "l", "t" nao comunicam nada sobre o proposito.',
      },
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# SISTEMA DE BIBLIOTECA — APOS REFATORACAO\n# Mesmo comportamento, codigo profissional\n\n# Constantes nomeadas (sem numeros magicos)\nLIMITE_EMPRESTIMOS_POR_USUARIO = 3\nPRAZO_DEVOLUCAO_DIAS = 14\nMULTA_POR_DIA_ATRASO = 0.50\n\n# Dados\nlivros = []\nusuarios = []\nemprestimos = []\n\n\n# CADASTRO DE LIVROS — responsabilidade unica: validar e registrar\ndef validar_dados_do_livro(titulo, autor, paginas, quantidade):\n    if titulo is None or autor is None:\n        return False\n    if paginas is None or paginas <= 0:\n        return False\n    if quantidade is None or quantidade <= 0:\n        return False\n    return True\n\ndef cadastrar_livro(titulo, autor, paginas, quantidade):\n    if not validar_dados_do_livro(titulo, autor, paginas, quantidade):\n        return False\n    novo_livro = {\n        "titulo": titulo,\n        "autor": autor,\n        "paginas": paginas,\n        "quantidade_total": quantidade,\n        "quantidade_disponivel": quantidade,\n    }\n    livros.append(novo_livro)\n    return True\n\n\n# BUSCA — responsabilidade unica: encontrar um registro\ndef buscar_usuario_por_id(id_usuario):\n    for usuario in usuarios:\n        if usuario["id"] == id_usuario:\n            return usuario\n    return None\n\ndef buscar_livro_por_titulo(titulo):\n    for livro in livros:\n        if livro["titulo"] == titulo:\n            return livro\n    return None\n\n\n# REGRAS DE NEGOCIO — responsabilidade unica: verificar permissoes\ndef contar_emprestimos_ativos_do_usuario(id_usuario):\n    return sum(1 for e in emprestimos if e["id_usuario"] == id_usuario and e["ativo"])\n\ndef usuario_pode_pegar_mais_livros(id_usuario):\n    return contar_emprestimos_ativos_do_usuario(id_usuario) < LIMITE_EMPRESTIMOS_POR_USUARIO\n\n\n# EMPRESTIMO — early return para validacoes\ndef emprestar_livro(id_usuario, titulo_livro):\n    usuario = buscar_usuario_por_id(id_usuario)\n    if usuario is None:\n        return "Erro: usuario nao encontrado"\n\n    livro = buscar_livro_por_titulo(titulo_livro)\n    if livro is None:\n        return "Erro: livro nao encontrado"\n\n    if not usuario_pode_pegar_mais_livros(id_usuario):\n        return f"Erro: limite de {LIMITE_EMPRESTIMOS_POR_USUARIO} emprestimos atingido"\n\n    if livro["quantidade_disponivel"] <= 0:\n        return "Erro: livro sem estoque disponivel"\n\n    livro["quantidade_disponivel"] -= 1\n    emprestimos.append({"id_usuario": id_usuario, "titulo": titulo_livro, "ativo": True, "dias_emprestado": 0})\n    return "Emprestimo realizado com sucesso"\n\n\n# DEVOLUCAO — responsabilidades separadas: encontrar, encerrar, calcular\ndef buscar_emprestimo_ativo(id_usuario, titulo_livro):\n    for emprestimo in emprestimos:\n        if (emprestimo["id_usuario"] == id_usuario\n                and emprestimo["titulo"] == titulo_livro\n                and emprestimo["ativo"]):\n            return emprestimo\n    return None\n\ndef calcular_multa_por_atraso(dias_emprestado):\n    if dias_emprestado <= PRAZO_DEVOLUCAO_DIAS:\n        return 0\n    dias_de_atraso = dias_emprestado - PRAZO_DEVOLUCAO_DIAS\n    return dias_de_atraso * MULTA_POR_DIA_ATRASO\n\ndef devolver_livro(id_usuario, titulo_livro):\n    emprestimo = buscar_emprestimo_ativo(id_usuario, titulo_livro)\n    if emprestimo is None:\n        return {"erro": "Emprestimo ativo nao encontrado", "multa": 0}\n\n    emprestimo["ativo"] = False\n\n    livro = buscar_livro_por_titulo(titulo_livro)\n    livro["quantidade_disponivel"] += 1\n\n    multa = calcular_multa_por_atraso(emprestimo["dias_emprestado"])\n    return {"sucesso": True, "multa": multa}',
        filename: 'biblioteca_depois.py',
        description:
          'A versao refatorada tem o mesmo comportamento mas e completamente diferente em qualidade. Cada funcao tem uma responsabilidade clara. Nomes descrevem o proposito. Constantes nomeadas eliminam numeros magicos. Early return simplifica o fluxo.',
      },
    },
    {
      type: 'text',
      content:
        '## O que mudou?\n\n### Nomenclatura\n| Antes | Depois | Por que melhor |\n|---|---|---|\n| `proc()` | `cadastrar_livro()` | Descreve a acao |\n| `pegar()` | `emprestar_livro()` | Vocabulario do negocio |\n| `dev()` | `devolver_livro()` | Sem abreviacao |\n| `uid`, `tid` | `id_usuario`, `titulo_livro` | Descritivo |\n| `x`, `l`, `t` | `usuario`, `livro`, `emprestimo` | Claro e preciso |\n| `cnt` | `contar_emprestimos_ativos_do_usuario()` | Funcao com nome descritivo |\n\n### SRP (Responsabilidade Unica)\n- `emprestar_livro()` nao busca mais — delega para `buscar_usuario_por_id()` e `buscar_livro_por_titulo()`\n- `devolver_livro()` nao calcula multa — delega para `calcular_multa_por_atraso()`\n- `usuario_pode_pegar_mais_livros()` encapsula a regra do limite\n\n### DRY\n- Busca de usuario e livro: funcoes reutilizaveis em vez de loops copiados\n- `contar_emprestimos_ativos_do_usuario()` pode ser usada em qualquer lugar\n\n### Numeros magicos eliminados\n- `3` → `LIMITE_EMPRESTIMOS_POR_USUARIO = 3`\n- `14` → `PRAZO_DEVOLUCAO_DIAS = 14`\n- `0.5` → `MULTA_POR_DIA_ATRASO = 0.50`\n\n### Early return\n- `emprestar_livro()`: valida primeiro, codigo principal por ultimo, sem indentacao excessiva',
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'Refatoracao nunca deve ser feita em um so passo em projetos reais. A tecnica correta e: (1) escreva testes primeiro, (2) faca uma pequena mudanca, (3) verifique que os testes ainda passam, (4) repita. Assim voce nunca quebra o que funcionava.',
    },
  ],
  challenges: [
    {
      id: 'refatoracao-c1',
      title: 'Refatore o Sistema de Estoque',
      description:
        'Refatore o sistema de estoque abaixo aplicando TODOS os conceitos do modulo: nomenclatura descritiva, SRP, DRY, constantes para numeros magicos e early return. O comportamento deve ser identico.',
      language: 'python',
      starterCode:
        '# Sistema de estoque para refatorar\n# Problemas: nomes ruins, funcao gigante, codigo duplicado, numeros magicos\n\nprod = []\n\ndef add(n, p, q):\n    if n != None and p != None and p > 0 and q != None and q >= 0:\n        prod.append({"n": n, "p": p, "q": q})\n        return True\n    return False\n\ndef vend(n, q):\n    item = None\n    for x in prod:\n        if x["n"] == n:\n            item = x\n    if item == None:\n        return "nao encontrado"\n    if item["q"] < q:\n        return "sem estoque"\n    if item["p"] * q > 10000:\n        return "valor muito alto, requer aprovacao"\n    item["q"] = item["q"] - q\n    if item["q"] < 5:\n        print("ALERTA: estoque baixo para " + n)\n    total = item["p"] * q\n    desconto = 0\n    if q >= 10:\n        desconto = total * 0.1\n    if q >= 50:\n        desconto = total * 0.2\n    return total - desconto\n\ndef rel():\n    for x in prod:\n        status = "OK"\n        if x["q"] < 5:\n            status = "BAIXO"\n        if x["q"] == 0:\n            status = "ZERADO"\n        print(x["n"] + " | R$" + str(x["p"]) + " | qtd: " + str(x["q"]) + " | " + status)\n\n# Teste\nadd("Caderno", 15.90, 20)\nadd("Caneta", 3.50, 3)\nadd("Borracha", 2.00, 0)\nprint(vend("Caderno", 12))\nrel()\n',
      solution:
        'ESTOQUE_MINIMO_ALERTA = 5\nLIMITE_VENDA_SEM_APROVACAO = 10000\nQUANTIDADE_DESCONTO_BASICO = 10\nQUANTIDADE_DESCONTO_AVANCADO = 50\nPERCENTUAL_DESCONTO_BASICO = 0.10\nPERCENTUAL_DESCONTO_AVANCADO = 0.20\n\nprodutos = []\n\ndef validar_dados_do_produto(nome, preco, quantidade):\n    if nome is None or preco is None or preco <= 0:\n        return False\n    if quantidade is None or quantidade < 0:\n        return False\n    return True\n\ndef cadastrar_produto(nome, preco, quantidade):\n    if not validar_dados_do_produto(nome, preco, quantidade):\n        return False\n    produtos.append({"nome": nome, "preco": preco, "quantidade": quantidade})\n    return True\n\ndef buscar_produto_por_nome(nome):\n    for produto in produtos:\n        if produto["nome"] == nome:\n            return produto\n    return None\n\ndef calcular_desconto(quantidade, valor_total):\n    if quantidade >= QUANTIDADE_DESCONTO_AVANCADO:\n        return valor_total * PERCENTUAL_DESCONTO_AVANCADO\n    if quantidade >= QUANTIDADE_DESCONTO_BASICO:\n        return valor_total * PERCENTUAL_DESCONTO_BASICO\n    return 0\n\ndef determinar_status_estoque(quantidade):\n    if quantidade == 0:\n        return "ZERADO"\n    if quantidade < ESTOQUE_MINIMO_ALERTA:\n        return "BAIXO"\n    return "OK"\n\ndef vender_produto(nome, quantidade):\n    produto = buscar_produto_por_nome(nome)\n    if produto is None:\n        return "Produto nao encontrado"\n    if produto["quantidade"] < quantidade:\n        return "Estoque insuficiente"\n    valor_total = produto["preco"] * quantidade\n    if valor_total > LIMITE_VENDA_SEM_APROVACAO:\n        return "Valor muito alto, requer aprovacao"\n    produto["quantidade"] -= quantidade\n    if produto["quantidade"] < ESTOQUE_MINIMO_ALERTA:\n        print(f"ALERTA: estoque baixo para {nome}")\n    desconto = calcular_desconto(quantidade, valor_total)\n    return valor_total - desconto\n\ndef gerar_relatorio_de_estoque():\n    for produto in produtos:\n        status = determinar_status_estoque(produto["quantidade"])\n        print(f"{produto[\'nome\']} | R${produto[\'preco\']:.2f} | qtd: {produto[\'quantidade\']} | {status}")\n\ncadastrar_produto("Caderno", 15.90, 20)\ncadastrar_produto("Caneta", 3.50, 3)\ncadastrar_produto("Borracha", 2.00, 0)\nprint(vender_produto("Caderno", 12))\ngerar_relatorio_de_estoque()',
      hints: [
        'Comece pelos nomes: "prod" -> "produtos", "add" -> "cadastrar_produto", "vend" -> "vender_produto", "rel" -> "gerar_relatorio_de_estoque".',
        'A funcao "vend" faz busca, validacao, desconto e venda — separe em: "buscar_produto_por_nome", "calcular_desconto", e a propria funcao de venda.',
        'Identifique os numeros magicos: 5 (estoque minimo), 10000 (limite), 10 e 50 (quantidades de desconto), 0.1 e 0.2 (percentuais).',
      ],
    },
  ],
};
