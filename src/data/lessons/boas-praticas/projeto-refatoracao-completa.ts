import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'projeto-refatoracao-completa',
  moduleId: 'boas-praticas',
  title: 'Projeto: Refatoracao Completa',
  description: 'Receba um projeto mal organizado e transforme em codigo profissional',
  order: 5,
  type: 'lesson',
  estimatedMinutes: 50,
  sections: [
    {
      type: 'text',
      content:
        '## Projeto: Refatoracao Completa\n\nNeste projeto voce vai receber um sistema funcional mas mal organizado — como acontece em projetos reais legados. Sua missao: aplicar todas as boas praticas aprendidas sem quebrar o comportamento existente.\n\nIsso e o que desenvolvedores seniors fazem quando entram em um time novo.\n\n### O sistema\n\nUm sistema de gerenciamento de notas escolares. Ele:\n- Cadastra alunos com nome e matricula\n- Registra notas (provas e trabalhos)\n- Calcula medias e determina aprovacao/reprovacao\n- Busca notas por aluno no banco de dados\n\n### Os problemas (que voce vai resolver)\n\n1. **Nomes de 1 letra** — `a`, `b`, `c`, `lst`, `n`, `r`\n2. **Funcao "deus" de 40 linhas** — faz tudo ao mesmo tempo\n3. **Numeros magicos** — `70` (nota de aprovacao), `3` (max tentativas)\n4. **Queries SQL inline** — espalhadas no meio da logica\n5. **Validacao duplicada** — repetida em 2 lugares\n6. **Comentarios descrevendo O QUE** — em vez de POR QUE',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# SISTEMA DE NOTAS — ANTES (70 linhas, 1 arquivo, caos total)\nimport sqlite3\n\ndef proc(n, m):\n    # processa aluno\n    if n == None or len(n) < 2:\n        return False\n    if m == None or len(m) != 6:\n        return False\n    c = sqlite3.connect("escola.db")\n    c.execute("INSERT INTO alunos (nome, matricula) VALUES (?,?)", (n, m))\n    c.commit()\n    c.close()\n    return True\n\ndef calc(a, lst):\n    # calcula resultado do aluno\n    if a == None:\n        return None\n    if lst == None or len(lst) == 0:\n        return None\n    c = sqlite3.connect("escola.db")\n    r = c.execute("SELECT nome FROM alunos WHERE matricula=?", (a,)).fetchone()\n    if r == None:\n        c.close()\n        return None\n    # calcula media\n    t = 0\n    cnt = 0\n    for x in lst:\n        if x["tipo"] == "prova":\n            t = t + x["nota"] * 0.6\n            cnt = cnt + 0.6\n        elif x["tipo"] == "trabalho":\n            t = t + x["nota"] * 0.4\n            cnt = cnt + 0.4\n    if cnt == 0:\n        return None\n    med = t / cnt\n    # salva notas\n    for x in lst:\n        c.execute(\n            "INSERT INTO notas (matricula, tipo, valor) VALUES (?,?,?)",\n            (a, x["tipo"], x["nota"])\n        )\n    c.commit()\n    # determina situacao\n    if med >= 70:\n        sit = "aprovado"\n    elif med >= 50:\n        sit = "recuperacao"\n    else:\n        sit = "reprovado"\n    c.execute(\n        "UPDATE alunos SET situacao=?, media=? WHERE matricula=?",\n        (sit, med, a)\n    )\n    c.commit()\n    c.close()\n    return {"aluno": r[0], "media": round(med, 1), "situacao": sit}\n\ndef get(a):\n    if a == None or len(a) != 6:\n        return None\n    c = sqlite3.connect("escola.db")\n    r = c.execute(\n        "SELECT tipo, valor FROM notas WHERE matricula=?", (a,)\n    ).fetchall()\n    c.close()\n    return r',
        filename: 'notas_antes.py',
        description:
          'O sistema legado. Funciona — mas e impossivel de manter. Nomes como "proc", "calc", "get", "a", "b", "c", "lst", "r", "t", "med", "sit" nao comunicam nada. A funcao calc() faz busca, calculo, persistencia e determinacao de situacao ao mesmo tempo.',
      },
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'Processo de refatoracao segura: (1) identifique os casos de borda e o comportamento esperado; (2) renomeie variaveis sem mudar logica; (3) extraia funcoes sem mudar logica; (4) reorganize a estrutura; (5) aplique padroes; (6) verifique que o comportamento e identico.',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# SISTEMA DE NOTAS — DEPOIS (4 arquivos especializados)\n\n# --- constants.py ---\nNOTA_MINIMA_APROVACAO = 70\nNOTA_MINIMA_RECUPERACAO = 50\nPESO_PROVA = 0.6\nPESO_TRABALHO = 0.4\nTAMANHO_MATRICULA = 6\nNOME_MINIMO_CARACTERES = 2\n\n\n# --- repository.py ---\nimport sqlite3\n\nclass NotasRepository:\n    def __init__(self, conn):\n        self.conn = conn\n\n    def buscar_aluno_por_matricula(self, matricula: str):\n        return self.conn.execute(\n            "SELECT nome FROM alunos WHERE matricula=?", (matricula,)\n        ).fetchone()\n\n    def cadastrar_aluno(self, nome: str, matricula: str):\n        self.conn.execute(\n            "INSERT INTO alunos (nome, matricula) VALUES (?,?)", (nome, matricula)\n        )\n        self.conn.commit()\n\n    def salvar_notas(self, matricula: str, notas: list):\n        for nota in notas:\n            self.conn.execute(\n                "INSERT INTO notas (matricula, tipo, valor) VALUES (?,?,?)",\n                (matricula, nota["tipo"], nota["nota"])\n            )\n        self.conn.commit()\n\n    def atualizar_situacao_do_aluno(self, matricula: str, situacao: str, media: float):\n        self.conn.execute(\n            "UPDATE alunos SET situacao=?, media=? WHERE matricula=?",\n            (situacao, media, matricula)\n        )\n        self.conn.commit()\n\n    def buscar_notas_do_aluno(self, matricula: str):\n        return self.conn.execute(\n            "SELECT tipo, valor FROM notas WHERE matricula=?", (matricula,)\n        ).fetchall()\n\n\n# --- service.py ---\nfrom constants import NOTA_MINIMA_APROVACAO, NOTA_MINIMA_RECUPERACAO\nfrom constants import PESO_PROVA, PESO_TRABALHO\n\ndef calcular_media_ponderada(notas: list) -> float:\n    soma_ponderada = 0\n    soma_pesos = 0\n    for nota in notas:\n        if nota["tipo"] == "prova":\n            soma_ponderada += nota["nota"] * PESO_PROVA\n            soma_pesos += PESO_PROVA\n        elif nota["tipo"] == "trabalho":\n            soma_ponderada += nota["nota"] * PESO_TRABALHO\n            soma_pesos += PESO_TRABALHO\n    if soma_pesos == 0:\n        return 0\n    return soma_ponderada / soma_pesos\n\ndef determinar_situacao_do_aluno(media: float) -> str:\n    if media >= NOTA_MINIMA_APROVACAO:\n        return "aprovado"\n    if media >= NOTA_MINIMA_RECUPERACAO:\n        return "recuperacao"\n    return "reprovado"\n\n\n# --- main.py ---\nfrom constants import TAMANHO_MATRICULA, NOME_MINIMO_CARACTERES\n\ndef matricula_e_valida(matricula: str) -> bool:\n    return matricula is not None and len(matricula) == TAMANHO_MATRICULA\n\ndef nome_e_valido(nome: str) -> bool:\n    return nome is not None and len(nome) >= NOME_MINIMO_CARACTERES\n\ndef cadastrar_aluno(nome: str, matricula: str, repo) -> bool:\n    if not nome_e_valido(nome) or not matricula_e_valida(matricula):\n        return False\n    repo.cadastrar_aluno(nome, matricula)\n    return True\n\ndef registrar_notas_e_calcular_resultado(matricula: str, notas: list, repo):\n    if not matricula_e_valida(matricula) or not notas:\n        return None\n    aluno = repo.buscar_aluno_por_matricula(matricula)\n    if aluno is None:\n        return None\n    media = calcular_media_ponderada(notas)\n    situacao = determinar_situacao_do_aluno(media)\n    repo.salvar_notas(matricula, notas)\n    repo.atualizar_situacao_do_aluno(matricula, situacao, media)\n    return {"aluno": aluno[0], "media": round(media, 1), "situacao": situacao}',
        filename: 'notas_depois.py',
        description:
          'O sistema refatorado. 4 arquivos com responsabilidades claras: constants.py (todas as "magicas"), repository.py (todas as queries SQL), service.py (calculos e regras), main.py (validacoes e orquestracao). O comportamento e identico — a manutenibilidade e incomparavelmente melhor.',
      },
    },
    {
      type: 'text',
      content:
        '## O que mudou?\n\n**Antes:** 70 linhas em 1 arquivo, 0 funcoes separadas, nomes de 1 letra, 4 numeros magicos, SQL espalhado.\n\n**Depois:** 4 arquivos especializados, 8 funcoes com nomes claros, 5 constantes nomeadas, Repository isolando o banco.\n\nO comportamento e identico. A manutenibilidade e 10x melhor.\n\n### Comparativo de nomes\n\n| Antes | Depois |\n|---|---|\n| `proc(n, m)` | `cadastrar_aluno(nome, matricula, repo)` |\n| `calc(a, lst)` | `registrar_notas_e_calcular_resultado(matricula, notas, repo)` |\n| `get(a)` | `buscar_notas_do_aluno(matricula)` |\n| `t`, `cnt`, `med`, `sit` | `soma_ponderada`, `soma_pesos`, `media`, `situacao` |\n| `70`, `50`, `0.6`, `0.4` | `NOTA_MINIMA_APROVACAO`, `NOTA_MINIMA_RECUPERACAO`, `PESO_PROVA`, `PESO_TRABALHO` |',
    },
  ],
  challenges: [
    {
      id: 'refatoracao-c1',
      title: 'Refatore o Sistema de Notas',
      description:
        'Aplique todas as boas praticas ao sistema legado: renomeie variaveis e funcoes, extraia a funcao "deus" em funcoes menores, adicione constantes nomeadas para os numeros magicos, e extraia um Repository para as queries SQL.',
      language: 'python',
      starterCode:
        '# Sistema de notas para refatorar — aplique todas as boas praticas\nimport sqlite3\n\ndef proc(n, m):\n    if n == None or len(n) < 2:\n        return False\n    if m == None or len(m) != 6:\n        return False\n    c = sqlite3.connect("escola.db")\n    c.execute("INSERT INTO alunos (nome, matricula) VALUES (?,?)", (n, m))\n    c.commit()\n    c.close()\n    return True\n\ndef calc(a, lst):\n    if a == None:\n        return None\n    if lst == None or len(lst) == 0:\n        return None\n    c = sqlite3.connect("escola.db")\n    r = c.execute("SELECT nome FROM alunos WHERE matricula=?", (a,)).fetchone()\n    if r == None:\n        c.close()\n        return None\n    t = 0\n    cnt = 0\n    for x in lst:\n        if x["tipo"] == "prova":\n            t = t + x["nota"] * 0.6\n            cnt = cnt + 0.6\n        elif x["tipo"] == "trabalho":\n            t = t + x["nota"] * 0.4\n            cnt = cnt + 0.4\n    if cnt == 0:\n        return None\n    med = t / cnt\n    for x in lst:\n        c.execute(\n            "INSERT INTO notas (matricula, tipo, valor) VALUES (?,?,?)",\n            (a, x["tipo"], x["nota"])\n        )\n    c.commit()\n    if med >= 70:\n        sit = "aprovado"\n    elif med >= 50:\n        sit = "recuperacao"\n    else:\n        sit = "reprovado"\n    c.execute(\n        "UPDATE alunos SET situacao=?, media=? WHERE matricula=?",\n        (sit, med, a)\n    )\n    c.commit()\n    c.close()\n    return {"aluno": r[0], "media": round(med, 1), "situacao": sit}\n\ndef get(a):\n    if a == None or len(a) != 6:\n        return None\n    c = sqlite3.connect("escola.db")\n    r = c.execute("SELECT tipo, valor FROM notas WHERE matricula=?", (a,)).fetchall()\n    c.close()\n    return r\n',
      solution:
        '# Constantes (sem numeros magicos)\nNOTA_MINIMA_APROVACAO = 70\nNOTA_MINIMA_RECUPERACAO = 50\nPESO_PROVA = 0.6\nPESO_TRABALHO = 0.4\nTAMANHO_MATRICULA = 6\nNOME_MINIMO_CARACTERES = 2\n\nimport sqlite3\n\n# Repository: todas as queries SQL em um unico lugar\nclass NotasRepository:\n    def __init__(self, conn):\n        self.conn = conn\n\n    def buscar_aluno_por_matricula(self, matricula):\n        return self.conn.execute(\n            "SELECT nome FROM alunos WHERE matricula=?", (matricula,)\n        ).fetchone()\n\n    def cadastrar_aluno(self, nome, matricula):\n        self.conn.execute(\n            "INSERT INTO alunos (nome, matricula) VALUES (?,?)", (nome, matricula)\n        )\n        self.conn.commit()\n\n    def salvar_notas(self, matricula, notas):\n        for nota in notas:\n            self.conn.execute(\n                "INSERT INTO notas (matricula, tipo, valor) VALUES (?,?,?)",\n                (matricula, nota["tipo"], nota["nota"])\n            )\n        self.conn.commit()\n\n    def atualizar_situacao_do_aluno(self, matricula, situacao, media):\n        self.conn.execute(\n            "UPDATE alunos SET situacao=?, media=? WHERE matricula=?",\n            (situacao, media, matricula)\n        )\n        self.conn.commit()\n\n    def buscar_notas_do_aluno(self, matricula):\n        return self.conn.execute(\n            "SELECT tipo, valor FROM notas WHERE matricula=?", (matricula,)\n        ).fetchall()\n\n\n# Validacoes (sem duplicacao)\ndef matricula_e_valida(matricula):\n    return matricula is not None and len(matricula) == TAMANHO_MATRICULA\n\ndef nome_e_valido(nome):\n    return nome is not None and len(nome) >= NOME_MINIMO_CARACTERES\n\n# Calculos (responsabilidade unica)\ndef calcular_media_ponderada(notas):\n    soma_ponderada = 0\n    soma_pesos = 0\n    for nota in notas:\n        if nota["tipo"] == "prova":\n            soma_ponderada += nota["nota"] * PESO_PROVA\n            soma_pesos += PESO_PROVA\n        elif nota["tipo"] == "trabalho":\n            soma_ponderada += nota["nota"] * PESO_TRABALHO\n            soma_pesos += PESO_TRABALHO\n    if soma_pesos == 0:\n        return 0\n    return soma_ponderada / soma_pesos\n\ndef determinar_situacao_do_aluno(media):\n    if media >= NOTA_MINIMA_APROVACAO:\n        return "aprovado"\n    if media >= NOTA_MINIMA_RECUPERACAO:\n        return "recuperacao"\n    return "reprovado"\n\n# Funcoes publicas (orquestram o fluxo)\ndef cadastrar_aluno(nome, matricula, repo):\n    if not nome_e_valido(nome) or not matricula_e_valida(matricula):\n        return False\n    repo.cadastrar_aluno(nome, matricula)\n    return True\n\ndef registrar_notas_e_calcular_resultado(matricula, notas, repo):\n    if not matricula_e_valida(matricula) or not notas:\n        return None\n    aluno = repo.buscar_aluno_por_matricula(matricula)\n    if aluno is None:\n        return None\n    media = calcular_media_ponderada(notas)\n    situacao = determinar_situacao_do_aluno(media)\n    repo.salvar_notas(matricula, notas)\n    repo.atualizar_situacao_do_aluno(matricula, situacao, media)\n    return {"aluno": aluno[0], "media": round(media, 1), "situacao": situacao}\n\ndef buscar_notas_do_aluno(matricula, repo):\n    if not matricula_e_valida(matricula):\n        return None\n    return repo.buscar_notas_do_aluno(matricula)\n',
      hints: [
        'Comece so pelos nomes: renomeie proc->cadastrar_aluno, calc->registrar_notas_e_calcular_resultado, get->buscar_notas_do_aluno, e todas as variaveis internas',
        'Extraia funcoes antes de reorganizar arquivos: calcular_media_ponderada() e determinar_situacao_do_aluno() saem da funcao principal',
        'O Repository e o ultimo passo: so depois que o resto estiver limpo, mova todas as queries para a classe NotasRepository',
      ],
    },
    {
      id: 'refatoracao-c2',
      title: 'Code Review: 5 Problemas',
      description:
        'A funcao abaixo tem 5 problemas de qualidade. Escreva um comentario de code review profissional para cada problema, no estilo usado em Pull Requests reais (especifico, educativo e propondo uma correcao).',
      language: 'python',
      starterCode:
        '# Escreva 5 comentarios de code review profissional para esta funcao\ndef f(u, d):  # PROBLEMA 1: ???\n    result = None\n    ok = False\n    if u != None:  # PROBLEMA 2: ???\n        if d != None:\n            if d > 0:\n                # calcula desconto\n                total = u["saldo"] * d  # PROBLEMA 3: ????\n                result = u["saldo"] - total\n                ok = True\n    if ok == True:  # PROBLEMA 4: ???\n        u["saldo"] = result\n        u["transacoes"].append({"tipo": "desconto", "valor": total})\n    return result\n    # Sem tratamento de erro se u["saldo"] ou u["transacoes"] nao existirem  # PROBLEMA 5: ???\n',
      solution:
        '# PROBLEMA 1 — Nomenclatura ruim (funcao e parametros)\n# Code Review: "O nome `f` nao comunica nada sobre o proposito desta funcao,\n# e `u` e `d` sao igualmente opacos. Sugiro renomear para algo como\n# `aplicar_desconto_no_saldo(usuario, percentual_desconto)`. Isso elimina\n# a necessidade de ler o corpo inteiro da funcao para entender o que ela faz."\n\n# PROBLEMA 2 — Indentacao excessiva (violacao de early return)\n# Code Review: "Os tres niveis de if aninhado (`u != None`, `d != None`, `d > 0`)\n# criam a piramide da morte. Sugiro usar early return: valide as condicoes no inicio\n# e deixe o codigo principal no nivel mais raso. Ex:\n# if usuario is None or percentual_desconto is None or percentual_desconto <= 0:\n#     return None"\n\n# PROBLEMA 3 — Numero magico / nome enganoso\n# Code Review: "A variavel `total` e enganosa — ela nao e o total, e o valor do desconto.\n# Sugiro renomear para `valor_do_desconto`. Tambem, `d` deveria se chamar\n# `percentual_desconto` para deixar claro que e uma fracao (0.1 para 10%)."\n\n# PROBLEMA 4 — Comparacao desnecessaria com True\n# Code Review: "A condicao `if ok == True` e redundante em Python (e na maioria\n# das linguagens). Use simplesmente `if ok:`. Melhor ainda: com early return,\n# voce elimina a variavel `ok` completamente — se chegou ate aqui, o processamento\n# foi bem sucedido."\n\n# PROBLEMA 5 — Ausencia de tratamento de erro\n# Code Review: "Se `usuario` nao tiver as chaves `saldo` ou `transacoes`, esta funcao\n# vai lancar um KeyError sem mensagem util. Sugiro validar as chaves necessarias\n# no inicio ou usar `.get()` com valores padrao. Ex:\n# saldo = usuario.get(\'saldo\')\n# if saldo is None:\n#     raise ValueError(f\'Usuario {usuario.get(\\\"id\\\", \\\"desconhecido\\\")} sem saldo\')"\n\n# Versao corrigida:\ndef aplicar_desconto_no_saldo(usuario, percentual_desconto):\n    if usuario is None or percentual_desconto is None or percentual_desconto <= 0:\n        return None\n    saldo_atual = usuario.get("saldo")\n    if saldo_atual is None:\n        raise ValueError("Usuario sem campo saldo")\n    valor_do_desconto = saldo_atual * percentual_desconto\n    novo_saldo = saldo_atual - valor_do_desconto\n    usuario["saldo"] = novo_saldo\n    usuario.setdefault("transacoes", []).append({"tipo": "desconto", "valor": valor_do_desconto})\n    return novo_saldo\n',
      hints: [
        'Um bom comentario de code review nomeia o problema, explica por que e um problema, e propoe uma solucao especifica',
        'Procure: nomes ruins, indentacao excessiva, numeros magicos ou nomes enganosos, comparacoes desnecessarias, falta de tratamento de erro',
        'O tom deve ser educativo e colaborativo, nao critico. "Sugiro..." e melhor que "Voce errou..."',
      ],
    },
  ],
};
