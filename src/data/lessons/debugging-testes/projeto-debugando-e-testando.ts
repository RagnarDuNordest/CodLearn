import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'projeto-debugando-e-testando',
  moduleId: 'debugging-testes',
  title: 'Projeto: Debugando e Testando Codigo Real',
  description: 'Coloque em pratica tudo que aprendeu: encontre bugs em um sistema de pedidos, corrija-os e escreva uma suite de testes completa',
  order: 5,
  type: 'lesson',
  estimatedMinutes: 60,
  sections: [
    {
      type: 'text',
      content:
        '## Projeto Final: Sistema de Pedidos\n\nVoce recebeu o codigo de um sistema de gerenciamento de pedidos que "funciona na maior parte do tempo". Sua missao:\n\n1. **Fase 1 — Explorar**: Leia o codigo e identifique pontos suspeitos\n2. **Fase 2 — Reproduzir**: Escreva entradas que revelam os bugs\n3. **Fase 3 — Corrigir**: Corrija cada bug encontrado\n4. **Fase 4 — Testar**: Escreva uma suite de testes com pytest\n\nO sistema tem ao menos **4 bugs conhecidos** — tente encontra-los todos antes de ver a solucao.\n\n### O que sera avaliado\n\n- Cobertura dos cenarios de teste (caminho feliz, erros, extremos)\n- Qualidade das correcoes (nao apenas fazer funcionar, mas fazer direito)\n- Clareza dos nomes dos testes\n- Uso correto de fixtures e pytest.raises',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# sistema_pedidos.py — codigo com bugs para encontrar\n\nclass Pedido:\n    def __init__(self, cliente):\n        self.cliente = cliente\n        self.itens = []\n        self.desconto = 0\n        self.status = "aberto"\n    \n    def adicionar_item(self, nome, preco, quantidade=1):\n        """Adiciona um item ao pedido."""\n        if quantidade <= 0:  # Bug 1: deveria ser < 0 (quantidade=0 deveria ser invalido)\n            raise ValueError("Quantidade deve ser positiva")\n        self.itens.append({\n            "nome": nome,\n            "preco": preco,\n            "quantidade": quantidade\n        })\n    \n    def aplicar_desconto(self, percentual):\n        """Aplica desconto percentual ao pedido (0-100)."""\n        if percentual < 0 or percentual > 100:\n            raise ValueError("Desconto deve estar entre 0 e 100")\n        self.desconto = percentual\n    \n    def calcular_total(self):\n        """Calcula o total do pedido com desconto."""\n        subtotal = 0\n        for item in self.itens:\n            subtotal = subtotal + item["preco"] * item["quantidade"]\n        # Bug 2: formula errada — deveria ser subtotal * (1 - self.desconto / 100)\n        total = subtotal * (1 - self.desconto)\n        return total\n    \n    def finalizar(self):\n        """Finaliza o pedido, mudando o status."""\n        if not self.itens:\n            raise ValueError("Nao e possivel finalizar pedido sem itens")\n        # Bug 3: nao verifica se o pedido ja foi finalizado\n        self.status = "finalizado"\n    \n    def adicionar_item_gratuito(self, nome):\n        """Adiciona um item gratuito ao pedido."""\n        # Bug 4: chama adicionar_item com preco=0, mas preco nao e validado\n        # um item com preco negativo pode ser adicionado via este metodo se alguem\n        # passar preco negativo diretamente em adicionar_item\n        self.itens.append({"nome": nome, "preco": 0, "quantidade": 1})',
        filename: 'sistema_pedidos.py',
        description:
          'Sistema com 4 bugs: (1) quantidade=0 nao gera erro, (2) formula de desconto errada, (3) pedido pode ser finalizado duas vezes, (4) preco negativo nao e validado em adicionar_item.',
      },
    },
    {
      type: 'text',
      content:
        '## Fase 1: Identifique os 4 Bugs\n\nAntes de ver a solucao, tente encontrar os bugs lendo o codigo:\n\n- **Bug 1**: Qual valor de quantidade deveria ser considerado invalido?\n- **Bug 2**: A formula `subtotal * (1 - self.desconto)` esta correta para um desconto percentual?\n- **Bug 3**: O que acontece se voce chamar `finalizar()` duas vezes?\n- **Bug 4**: O que acontece se voce chamar `adicionar_item("Produto", -50)`?\n\n## Fase 2: Escreva os Testes Primeiro\n\nAntes de corrigir, escreva testes que **falham** com o codigo atual mas **passam** com o codigo correto. Isso confirma que voce entendeu o problema e verifica a correcao.\n\n```python\n# Exemplo de teste que falha com o codigo bugado\ndef test_quantidade_zero_deve_ser_invalida():\n    pedido = Pedido("Cliente Teste")\n    with pytest.raises(ValueError):  # Bug 1: atualmente nao gera erro\n        pedido.adicionar_item("Produto", 10.0, quantidade=0)\n```',
    },
  ],
  challenges: [
    {
      id: 'projeto-debug-c1',
      title: 'Corrija e Teste o Sistema',
      description:
        'Corrija os 4 bugs no sistema de pedidos e escreva uma suite completa de testes com pytest. Sua suite deve ter pelo menos 10 testes cobrindo os bugs corrigidos, o caminho feliz e casos extremos.',
      language: 'python',
      starterCode:
        '# Corrija o codigo e escreva os testes\nimport pytest\n\nclass Pedido:\n    def __init__(self, cliente):\n        self.cliente = cliente\n        self.itens = []\n        self.desconto = 0\n        self.status = "aberto"\n    \n    def adicionar_item(self, nome, preco, quantidade=1):\n        if quantidade <= 0:  # Bug 1: 0 deve ser invalido, mas <= 0 so bloqueia negativo?\n            raise ValueError("Quantidade deve ser positiva")\n        # Bug 4: preco negativo nao e validado\n        self.itens.append({"nome": nome, "preco": preco, "quantidade": quantidade})\n    \n    def aplicar_desconto(self, percentual):\n        if percentual < 0 or percentual > 100:\n            raise ValueError("Desconto deve estar entre 0 e 100")\n        self.desconto = percentual\n    \n    def calcular_total(self):\n        subtotal = sum(i["preco"] * i["quantidade"] for i in self.itens)\n        total = subtotal * (1 - self.desconto)  # Bug 2: desconto nao e dividido por 100\n        return total\n    \n    def finalizar(self):\n        if not self.itens:\n            raise ValueError("Pedido sem itens nao pode ser finalizado")\n        # Bug 3: pode finalizar pedido ja finalizado\n        self.status = "finalizado"\n    \n    def adicionar_item_gratuito(self, nome):\n        self.itens.append({"nome": nome, "preco": 0, "quantidade": 1})\n\n# Escreva seus testes aqui:\n@pytest.fixture\ndef pedido_basico():\n    # TODO: crie um pedido com alguns itens\n    pass\n\n# TODO: escreva pelo menos 10 testes\n',
      solution:
        'import pytest\n\nclass Pedido:\n    def __init__(self, cliente):\n        self.cliente = cliente\n        self.itens = []\n        self.desconto = 0\n        self.status = "aberto"\n    \n    def adicionar_item(self, nome, preco, quantidade=1):\n        # Correcao Bug 1: quantidade=0 deve ser invalido\n        if quantidade < 1:\n            raise ValueError("Quantidade deve ser pelo menos 1")\n        # Correcao Bug 4: preco negativo nao deve ser aceito\n        if preco < 0:\n            raise ValueError("Preco nao pode ser negativo")\n        self.itens.append({"nome": nome, "preco": preco, "quantidade": quantidade})\n    \n    def aplicar_desconto(self, percentual):\n        if percentual < 0 or percentual > 100:\n            raise ValueError("Desconto deve estar entre 0 e 100")\n        self.desconto = percentual\n    \n    def calcular_total(self):\n        subtotal = sum(i["preco"] * i["quantidade"] for i in self.itens)\n        # Correcao Bug 2: desconto e percentual, precisa dividir por 100\n        total = subtotal * (1 - self.desconto / 100)\n        return total\n    \n    def finalizar(self):\n        if not self.itens:\n            raise ValueError("Pedido sem itens nao pode ser finalizado")\n        # Correcao Bug 3: verificar se ja foi finalizado\n        if self.status == "finalizado":\n            raise ValueError("Pedido ja foi finalizado")\n        self.status = "finalizado"\n    \n    def adicionar_item_gratuito(self, nome):\n        self.itens.append({"nome": nome, "preco": 0, "quantidade": 1})\n\n\n@pytest.fixture\ndef pedido_basico():\n    pedido = Pedido("Maria")\n    pedido.adicionar_item("Notebook", 3500.0, 1)\n    pedido.adicionar_item("Mouse", 150.0, 2)\n    return pedido\n\n# Testes do caminho feliz\ndef test_pedido_criado_com_status_aberto():\n    pedido = Pedido("Joao")\n    assert pedido.status == "aberto"\n    assert pedido.cliente == "Joao"\n\ndef test_adicionar_item_e_calcular_total(pedido_basico):\n    # 3500 + 150*2 = 3800\n    assert pedido_basico.calcular_total() == 3800.0\n\ndef test_desconto_aplicado_corretamente(pedido_basico):\n    pedido_basico.aplicar_desconto(10)\n    # 3800 * (1 - 10/100) = 3800 * 0.9 = 3420\n    assert pedido_basico.calcular_total() == 3420.0\n\ndef test_finalizar_muda_status(pedido_basico):\n    pedido_basico.finalizar()\n    assert pedido_basico.status == "finalizado"\n\ndef test_item_gratuito_nao_afeta_total(pedido_basico):\n    total_antes = pedido_basico.calcular_total()\n    pedido_basico.adicionar_item_gratuito("Brinde")\n    assert pedido_basico.calcular_total() == total_antes\n\n# Testes das correcoes de bugs\ndef test_bug1_quantidade_zero_invalida():\n    pedido = Pedido("Ana")\n    with pytest.raises(ValueError):\n        pedido.adicionar_item("Produto", 100.0, quantidade=0)\n\ndef test_bug2_desconto_percentual_correto():\n    pedido = Pedido("Carlos")\n    pedido.adicionar_item("Item", 100.0)\n    pedido.aplicar_desconto(20)  # 20%, nao 0.2\n    assert pedido.calcular_total() == 80.0  # Nao 100*(1-20) = -1900!\n\ndef test_bug3_finalizar_duas_vezes_gera_erro(pedido_basico):\n    pedido_basico.finalizar()\n    with pytest.raises(ValueError, match="ja foi finalizado"):\n        pedido_basico.finalizar()\n\ndef test_bug4_preco_negativo_invalido():\n    pedido = Pedido("Bruno")\n    with pytest.raises(ValueError):\n        pedido.adicionar_item("Produto", -50.0)\n\ndef test_pedido_vazio_nao_pode_ser_finalizado():\n    pedido = Pedido("Novo")\n    with pytest.raises(ValueError):\n        pedido.finalizar()\n',
      hints: [
        'Bug 1: quantidade <= 0 bloqueia negativos, mas 0 passa porque 0 <= 0 e True. Troque para quantidade < 1',
        'Bug 2: self.desconto guarda o percentual (ex: 20), mas a formula usa como decimal. Deve ser self.desconto / 100',
        'Bug 3: adicione "if self.status == finalizado: raise ValueError" antes de mudar o status',
        'Bug 4: adicione validacao "if preco < 0: raise ValueError" em adicionar_item',
      ],
    },
  ],
};
