import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'decomposicao-de-sistemas',
  moduleId: 'pensamento-sistemas',
  title: 'Decomposicao de Sistemas',
  description: 'Como quebrar um sistema grande em subsistemas menores, coesos e pouco acoplados',
  order: 1,
  type: 'lesson',
  estimatedMinutes: 25,
  sections: [
    {
      type: 'text',
      content:
        '## Por que decompor?\n\nNenhum engenheiro consegue pensar em todos os detalhes de um sistema grande ao mesmo tempo. A decomposicao e a tecnica de dividir um problema complexo em partes menores, cada uma com responsabilidade clara e bem delimitada.\n\nUm sistema de delivery como iFood ou Rappi tem milhoes de linhas de codigo. Nenhuma pessoa consegue entender tudo — mas qualquer engenheiro consegue entender o subsistema de pagamentos, ou o subsistema de rastreamento, ou o de catalogo de restaurantes.\n\n### Duas estrategias de decomposicao\n\n**Top-down (de cima para baixo)**\nVoce comeca do objetivo maior e vai quebrando em partes menores.\n```\nSistema de Delivery\n  ├── Subsistema de Usuarios\n  ├── Subsistema de Restaurantes\n  ├── Subsistema de Pedidos\n  │     ├── Carrinho de compras\n  │     ├── Calculo de frete\n  │     └── Confirmacao de pedido\n  ├── Subsistema de Pagamentos\n  └── Subsistema de Logistica\n        ├── Alocacao de entregadores\n        └── Rastreamento em tempo real\n```\nBom para sistemas novos onde voce conhece o dominio.\n\n**Bottom-up (de baixo para cima)**\nVoce comeca pelas pecas fundamentais e vai compondo sistemas maiores.\nBom para quando voce ja tem componentes existentes e quer construir algo maior em cima deles.',
    },
    {
      type: 'text',
      content:
        '## Coesao e Acoplamento\n\nEsses sao os dois criterios mais importantes para avaliar se uma decomposicao e boa.\n\n### Coesao: o que deve ficar JUNTO\nCoesao mede o quanto as partes de um componente estao relacionadas entre si. **Alta coesao = bom.** Um componente coeso faz uma coisa e a faz bem.\n\n- Alta coesao: `GerenciadorDePagamentos` — tudo dentro dele e sobre processar pagamentos\n- Baixa coesao: `UtilitariosGerais` — tem funcao de email, calculos de frete, validacao de CPF e envio de SMS misturados\n\n### Acoplamento: o que deve ficar SEPARADO\nAcoplamento mede o quanto um componente depende dos detalhes internos de outro. **Baixo acoplamento = bom.** Componentes pouco acoplados podem ser modificados sem quebrar os outros.\n\n- Alto acoplamento (ruim): `ServicoDeEmail` acessa diretamente a tabela `usuarios` no banco\n- Baixo acoplamento (bom): `ServicoDeEmail` recebe um endereco de email como parametro — nao sabe nada sobre como o usuario e armazenado\n\n### A regra pratica\n> **Coisas que mudam juntas devem ficar juntas. Coisas que mudam independentemente devem ficar separadas.**\n\nSe voce sempre edita o arquivo A quando edita o arquivo B, eles provavelmente deveriam ser um so componente. Se voce consegue modificar o modulo de pagamento sem tocar no modulo de logistica, seu acoplamento esta bom.',
    },
    {
      type: 'callout',
      calloutType: 'warning',
      content:
        'Armadilha comum: decompor DEMAIS. Ter 50 microcomponentes para um sistema simples cria complexidade desnecessaria. A decomposicao deve resolver um problema real — nao ser um exercicio academico. Se dois componentes sempre precisam ser modificados juntos, talvez devessem ser um so.',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# ANTES: Sistema monolitico — tudo na mesma classe\n# Problema: baixa coesao, alto acoplamento\nclass SistemaDeliveryMonolitico:\n    def cadastrar_usuario(self, nome, email): pass\n    def buscar_restaurantes_proximos(self, latitude, longitude): pass\n    def adicionar_item_ao_carrinho(self, usuario_id, item_id): pass\n    def calcular_frete(self, distancia_km): pass\n    def processar_pagamento_cartao(self, numero, cvv, valor): pass\n    def processar_pagamento_pix(self, chave_pix, valor): pass\n    def alocar_entregador(self, pedido_id): pass\n    def atualizar_localizacao_entregador(self, entregador_id, lat, lng): pass\n    def enviar_email_confirmacao(self, email, pedido_id): pass\n    def enviar_push_notification(self, usuario_id, mensagem): pass\n\n\n# DEPOIS: Decomposicao em subsistemas coesos\n# Cada classe tem alta coesao — so faz uma coisa\n\nclass RepositorioDeUsuarios:\n    """Responsabilidade: persistencia de dados de usuarios"""\n    def salvar(self, usuario): pass\n    def buscar_por_id(self, usuario_id): pass\n    def buscar_por_email(self, email): pass\n\nclass CatalogodeRestaurantes:\n    """Responsabilidade: busca e filtragem de restaurantes"""\n    def buscar_proximos(self, latitude, longitude, raio_km): pass\n    def buscar_por_categoria(self, categoria): pass\n    def obter_cardapio(self, restaurante_id): pass\n\nclass GerenciadorDePedidos:\n    """Responsabilidade: ciclo de vida de um pedido"""\n    def criar_carrinho(self, usuario_id): pass\n    def adicionar_item(self, carrinho_id, item_id, quantidade): pass\n    def calcular_total(self, carrinho_id): pass\n    def confirmar_pedido(self, carrinho_id): pass\n\nclass ProcessadorDePagamentos:\n    """Responsabilidade: processamento de diferentes formas de pagamento"""\n    def cobrar_cartao(self, dados_cartao, valor): pass\n    def cobrar_pix(self, chave_pix, valor): pass\n    def estornar(self, transacao_id): pass\n\nclass SistemaDeLogistica:\n    """Responsabilidade: entregadores e rastreamento"""\n    def alocar_entregador_disponivel(self, pedido_id): pass\n    def atualizar_posicao(self, entregador_id, latitude, longitude): pass\n    def calcular_tempo_estimado(self, origem, destino): pass\n\nclass ServicoDeNotificacoes:\n    """Responsabilidade: comunicacao com o usuario"""\n    def enviar_email(self, destinatario, assunto, corpo): pass\n    def enviar_push(self, usuario_id, titulo, mensagem): pass\n    def enviar_sms(self, telefone, mensagem): pass',
        filename: 'monolitico_vs_decomposto.py',
        description:
          'O sistema monolitico tem 10 metodos sem relacao entre si — baixa coesao. A versao decomposta tem 6 classes, cada uma com 3-4 metodos altamente relacionados. Se precisarmos mudar a logica de pagamento, tocamos apenas em ProcessadorDePagamentos — sem risco de quebrar o rastreamento.',
      },
    },
    {
      type: 'callout',
      calloutType: 'info',
      content:
        'Pergunta para testar sua decomposicao: "Se eu precisar trocar o sistema de pagamentos (ex: de Stripe para PagSeguro), quantos arquivos precisarei modificar?" Se a resposta for "muitos arquivos em varios subsistemas", seu acoplamento esta alto demais.',
    },
  ],
  challenges: [
    {
      id: 'decomposicao-c1',
      title: 'Refatore o Sistema de Biblioteca Monolitico',
      description:
        'A classe abaixo e um sistema de biblioteca com baixa coesao — mistura gerenciamento de livros, controle de emprestimos, cadastro de membros e envio de notificacoes. Refatore separando em classes coesas com responsabilidades claras. Mantenha toda a logica existente — apenas reorganize.',
      language: 'python',
      starterCode:
        '# Sistema de biblioteca monolitico — refatore separando em subsistemas\nclass BibliotecaMonolitica:\n    def __init__(self):\n        self.livros = {}        # {isbn: {"titulo": str, "autor": str, "disponivel": bool}}\n        self.membros = {}       # {membro_id: {"nome": str, "email": str}}\n        self.emprestimos = {}   # {emprestimo_id: {"livro_isbn": str, "membro_id": str, "data_devolucao": str}}\n        self.proximo_emprestimo_id = 1\n\n    # --- Operacoes de livros ---\n    def cadastrar_livro(self, isbn, titulo, autor):\n        self.livros[isbn] = {"titulo": titulo, "autor": autor, "disponivel": True}\n\n    def buscar_livro_por_isbn(self, isbn):\n        return self.livros.get(isbn)\n\n    def listar_livros_disponiveis(self):\n        return [l for l in self.livros.values() if l["disponivel"]]\n\n    # --- Operacoes de membros ---\n    def cadastrar_membro(self, membro_id, nome, email):\n        self.membros[membro_id] = {"nome": nome, "email": email}\n\n    def buscar_membro(self, membro_id):\n        return self.membros.get(membro_id)\n\n    # --- Operacoes de emprestimos ---\n    def realizar_emprestimo(self, isbn, membro_id, data_devolucao):\n        if isbn not in self.livros or not self.livros[isbn]["disponivel"]:\n            return {"sucesso": False, "mensagem": "Livro nao disponivel"}\n        if membro_id not in self.membros:\n            return {"sucesso": False, "mensagem": "Membro nao cadastrado"}\n        emprestimo_id = f"EMP{self.proximo_emprestimo_id:04d}"\n        self.emprestimos[emprestimo_id] = {\n            "livro_isbn": isbn, "membro_id": membro_id, "data_devolucao": data_devolucao\n        }\n        self.livros[isbn]["disponivel"] = False\n        self.proximo_emprestimo_id += 1\n        self._notificar_emprestimo(membro_id, isbn, data_devolucao)\n        return {"sucesso": True, "emprestimo_id": emprestimo_id}\n\n    def devolver_livro(self, emprestimo_id):\n        if emprestimo_id not in self.emprestimos:\n            return {"sucesso": False, "mensagem": "Emprestimo nao encontrado"}\n        isbn = self.emprestimos[emprestimo_id]["livro_isbn"]\n        self.livros[isbn]["disponivel"] = True\n        del self.emprestimos[emprestimo_id]\n        return {"sucesso": True, "mensagem": "Livro devolvido com sucesso"}\n\n    # --- Notificacoes ---\n    def _notificar_emprestimo(self, membro_id, isbn, data_devolucao):\n        membro = self.membros.get(membro_id)\n        livro = self.livros.get(isbn)\n        if membro and livro:\n            print(f"Email para {membro[\'email\']}: Voce pegou \'{livro[\'titulo\']}\'. Devolva ate {data_devolucao}")\n\n\n# TODO: Refatore criando as classes separadas:\n# - CatalogoLivros\n# - RegistroDeMembros\n# - GerenciadorDeEmprestimos\n# - ServicoDeNotificacoesBiblioteca\n# E uma classe BibliotecaRefatorada que as orquestra\n',
      solution:
        '# Subsistema 1: Catalogo de livros\nclass CatalogoLivros:\n    def __init__(self):\n        self.livros = {}  # {isbn: {"titulo": str, "autor": str, "disponivel": bool}}\n\n    def cadastrar(self, isbn, titulo, autor):\n        self.livros[isbn] = {"titulo": titulo, "autor": autor, "disponivel": True}\n\n    def buscar(self, isbn):\n        return self.livros.get(isbn)\n\n    def listar_disponiveis(self):\n        return [l for l in self.livros.values() if l["disponivel"]]\n\n    def marcar_indisponivel(self, isbn):\n        if isbn in self.livros:\n            self.livros[isbn]["disponivel"] = False\n\n    def marcar_disponivel(self, isbn):\n        if isbn in self.livros:\n            self.livros[isbn]["disponivel"] = True\n\n\n# Subsistema 2: Registro de membros\nclass RegistroDeMembros:\n    def __init__(self):\n        self.membros = {}  # {membro_id: {"nome": str, "email": str}}\n\n    def cadastrar(self, membro_id, nome, email):\n        self.membros[membro_id] = {"nome": nome, "email": email}\n\n    def buscar(self, membro_id):\n        return self.membros.get(membro_id)\n\n    def existe(self, membro_id):\n        return membro_id in self.membros\n\n\n# Subsistema 3: Gerenciamento de emprestimos\nclass GerenciadorDeEmprestimos:\n    def __init__(self):\n        self.emprestimos = {}  # {emprestimo_id: {...}}\n        self.proximo_id = 1\n\n    def criar(self, isbn, membro_id, data_devolucao):\n        emprestimo_id = f"EMP{self.proximo_id:04d}"\n        self.emprestimos[emprestimo_id] = {\n            "livro_isbn": isbn, "membro_id": membro_id, "data_devolucao": data_devolucao\n        }\n        self.proximo_id += 1\n        return emprestimo_id\n\n    def encerrar(self, emprestimo_id):\n        if emprestimo_id not in self.emprestimos:\n            return None\n        isbn = self.emprestimos[emprestimo_id]["livro_isbn"]\n        del self.emprestimos[emprestimo_id]\n        return isbn  # retorna isbn para o catalogo atualizar disponibilidade\n\n    def buscar(self, emprestimo_id):\n        return self.emprestimos.get(emprestimo_id)\n\n\n# Subsistema 4: Notificacoes\nclass ServicoDeNotificacoesBiblioteca:\n    def notificar_emprestimo(self, email, titulo_livro, data_devolucao):\n        print(f"Email para {email}: Voce pegou \'{titulo_livro}\'. Devolva ate {data_devolucao}")\n\n    def notificar_devolucao(self, email, titulo_livro):\n        print(f"Email para {email}: Obrigado por devolver \'{titulo_livro}\'!")\n\n\n# Orquestrador: coordena os subsistemas\nclass BibliotecaRefatorada:\n    def __init__(self):\n        self.catalogo = CatalogoLivros()\n        self.membros = RegistroDeMembros()\n        self.emprestimos = GerenciadorDeEmprestimos()\n        self.notificacoes = ServicoDeNotificacoesBiblioteca()\n\n    def cadastrar_livro(self, isbn, titulo, autor):\n        self.catalogo.cadastrar(isbn, titulo, autor)\n\n    def cadastrar_membro(self, membro_id, nome, email):\n        self.membros.cadastrar(membro_id, nome, email)\n\n    def realizar_emprestimo(self, isbn, membro_id, data_devolucao):\n        livro = self.catalogo.buscar(isbn)\n        if not livro or not livro["disponivel"]:\n            return {"sucesso": False, "mensagem": "Livro nao disponivel"}\n        if not self.membros.existe(membro_id):\n            return {"sucesso": False, "mensagem": "Membro nao cadastrado"}\n        emprestimo_id = self.emprestimos.criar(isbn, membro_id, data_devolucao)\n        self.catalogo.marcar_indisponivel(isbn)\n        membro = self.membros.buscar(membro_id)\n        self.notificacoes.notificar_emprestimo(membro["email"], livro["titulo"], data_devolucao)\n        return {"sucesso": True, "emprestimo_id": emprestimo_id}\n\n    def devolver_livro(self, emprestimo_id):\n        isbn = self.emprestimos.encerrar(emprestimo_id)\n        if isbn is None:\n            return {"sucesso": False, "mensagem": "Emprestimo nao encontrado"}\n        self.catalogo.marcar_disponivel(isbn)\n        return {"sucesso": True, "mensagem": "Livro devolvido com sucesso"}\n\n\n# Teste\nbiblioteca = BibliotecaRefatorada()\nbiblioteca.cadastrar_livro("978-1", "Clean Code", "Robert Martin")\nbiblioteca.cadastrar_membro("M001", "Alice", "alice@email.com")\nresultado = biblioteca.realizar_emprestimo("978-1", "M001", "2026-04-15")\nprint(resultado)  # {"sucesso": True, "emprestimo_id": "EMP0001"}\n',
      hints: [
        'Identifique os grupos de metodos que se comunicam com os mesmos dados: metodos que mexem em self.livros ficam juntos, metodos que mexem em self.membros ficam juntos.',
        'O GerenciadorDeEmprestimos nao deve saber que o livro existe — ele so gerencia ids. O catalogo e o gerenciador se comunicam via o orquestrador.',
        'A classe orquestradora (BibliotecaRefatorada) nao tem logica propria — ela apenas coordena as chamadas entre os subsistemas na ordem certa.',
      ],
    },
  ],
};
